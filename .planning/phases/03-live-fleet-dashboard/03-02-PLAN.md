---
phase: 03-live-fleet-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/dashboard/src/app/features/fleet-map/fleet-map.component.ts
  - packages/dashboard/src/app/features/fleet-map/fleet-map.component.html
  - packages/dashboard/src/app/features/dashboard/dashboard.component.ts
  - packages/dashboard/src/app/features/dashboard/dashboard.component.html
  - packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-grid.component.ts
  - packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-grid.component.html
  - packages/dashboard/src/app/layout/shell/shell.component.ts
  - packages/dashboard/src/app/layout/shell/shell.component.html
  - packages/dashboard/src/app/core/services/alert.service.ts
autonomous: false
requirements:
  - FLEET-01
  - FLEET-02
  - FLEET-03

must_haves:
  truths:
    - "Map shows stale vehicles at last known position with grey dimmed marker (opacity 0.5)"
    - "Stale marker tooltip reads 'Last seen N min ago' on hover"
    - "fitBounds() fires only on first load — does not re-center map on telemetry updates"
    - "Map shows 'No vehicles with GPS data' centered overlay when fleet has no GPS records"
    - "Dashboard shows LoadingSpinnerComponent while vehicleResource is loading"
    - "Dashboard shows empty state 'No vehicles in your fleet' + '+ Add Vehicle' CTA when resolved with empty array"
    - "Connection pill is visible in shell header and reflects TelemetryService.connectionStatus"
    - "Connection status change emits toast — 'Connection lost' on disconnect, 'Live data restored' on reconnect"
    - "ShellComponent does NOT call loadVehicles(), loadFleets(), or loadOrganizations() in ngOnInit"
    - "Vehicle card click on /dashboard navigates to /vehicle/:id"
  artifacts:
    - path: "packages/dashboard/src/app/features/fleet-map/fleet-map.component.ts"
      provides: "4-state getVehicleState() with stale priority, initialFitDone guard, stale tooltip"
      contains: "initialFitDone"
    - path: "packages/dashboard/src/app/features/dashboard/dashboard.component.html"
      provides: "Loading/empty/populated states driven by vehicleResource.isLoading"
    - path: "packages/dashboard/src/app/layout/shell/shell.component.ts"
      provides: "VdConnectionPillComponent wired to connectionStatus, toast effect on status change"
      contains: "vd-connection-pill"
    - path: "packages/dashboard/src/app/core/services/alert.service.ts"
      provides: "Public pushAlert() method for connection toasts"
      contains: "pushAlert"
  key_links:
    - from: "FleetMapComponent.getVehicleState()"
      to: "vehicle.latestTelemetry.timestamp"
      via: "Date.now() - new Date(ts).getTime() > 15 * 60 * 1000"
      pattern: "15 \\* 60 \\* 1000"
    - from: "ShellComponent template"
      to: "TelemetryService.connectionStatus"
      via: "[state]=\"connectionStatus()\""
      pattern: "connectionStatus\\(\\)"
    - from: "DashboardComponent template"
      to: "vehicleResource.isLoading"
      via: "@if (isLoading())"
      pattern: "isLoading\\(\\)"
---

<objective>
Wire all UI integration points: map stale state, dashboard loading/empty states, shell connection pill, and connection toast.

Purpose: The Angular data layer (plan 03-01) is now reactive. This plan connects the UI to that data: map markers gain the stale state, fitBounds stops thrashing on updates, the dashboard shows correct loading/empty/populated states, the connection pill appears in the shell header, and status transitions trigger toasts.

Output:
- FleetMapComponent: stale state in getVehicleState() + getMarkerIcon(), initialFitDone guard, stale tooltip
- DashboardComponent + VehicleGridComponent: resource-driven loading/empty states (per D-23/D-25)
- AlertService: new public pushAlert() method for connection toasts
- ShellComponent: VdConnectionPillComponent wired, ngOnInit manual load chain removed, toast effect added
- Wave 0 test stubs from plan 03-01 must now be turned green
</objective>

<execution_context>
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-01-SUMMARY.md
</execution_context>

<context>
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/ROADMAP.md
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-CONTEXT.md
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-RESEARCH.md
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-PATTERNS.md
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-UI-SPEC.md
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Read these files before touching any modified file. -->

From packages/dashboard/src/app/shared/ui/connection-pill/connection-pill.component.ts:
  Selector: vd-connection-pill
  Input: state: ConnectionState ('connected' | 'reconnecting' | 'disconnected')
  Input: label: string | null (optional)
  Usage: <vd-connection-pill [state]="connectionStatus()" />

From packages/dashboard/src/app/shared/components/loading-spinner/loading-spinner.component.ts:
  Inputs: message: string (default 'Loading...'), fullPage: boolean (default false)

From packages/dashboard/src/app/shared/components/toast/toast.component.ts (VERIFIED):
  Selector: app-toast
  Driven by AlertService.activeAlerts() computed signal — ToastComponent reads from AlertService directly.
  ToastComponent has NO inputs and accepts NO direct method calls.
  Auto-dismiss is controlled by ALERT_AUTO_DISMISS[severity]: critical=0ms, warning=12000ms, info=8000ms.
  To display a connection toast, push an Alert into AlertService via the new pushAlert() method (see below).

From packages/dashboard/src/app/core/services/alert.service.ts (VERIFIED — read directly):
  AlertService has NO public addAlert() method. createAlert() is private.
  AlertType union already includes 'connection' (from alert.model.ts).
  Alert interface requires: id, vehicleId, type, severity, message, timestamp, status.
  For connection alerts, use vehicleId: 'fleet' as a non-vehicle sentinel value.
  A new public pushAlert() method must be added to AlertService before ShellComponent can use it.

Toast API to implement in alert.service.ts:
  Add this public method to AlertService (after the clearAllAlerts() method):
    pushAlert(message: string, severity: AlertSeverity, type: AlertType = 'connection'): void {
      this._alerts.update(alerts => {
        const trimmed = alerts.length >= 200 ? alerts.slice(-199) : alerts;
        const id = `alert-${Date.now()}-${++alertIdCounter}`;
        return [...trimmed, {
          id,
          vehicleId: 'fleet',
          type,
          severity,
          message,
          timestamp: new Date(),
          status: 'active' as AlertStatus,
        }];
      });
    }
  Note: alertIdCounter is the module-level let variable already declared at line 9 of alert.service.ts.
  Note: ALERT_AUTO_DISMISS.info = 8000ms. UI-SPEC calls for 3000ms reconnect dismiss — 8000ms is the closest
  available without modifying ALERT_AUTO_DISMISS. Use 'info' severity for reconnect toast (8000ms auto-dismiss).
  Use 'warning' severity for disconnect toast (12000ms auto-dismiss — persistent enough for the user to notice).

From packages/dashboard/src/app/features/fleet-map/fleet-map.component.ts:
  Existing getVehicleState() returns: 'offline' | 'running' | 'alert' — 3 states
  Existing getMarkerIcon() uses colors map: { offline: '#6b7280', running: '#22c55e', alert: '#ef4444' }
  Existing updateMarkers() has unconditional fitBounds() block (lines ~144–150)
  Effect in constructor watches vehiclesWithHealth signal (1s debounce via setTimeout) → calls updateMarkers()
  initMap() sets default center [32.0853, 34.7818] zoom 12 — already in place

From packages/dashboard/src/app/layout/shell/shell.component.ts:
  Existing ngOnInit loads: organizationService.loadOrganizations() → fleetService.loadFleets() → vehicleService.loadVehicles() → vehicleService.loadInitialTelemetry()
  This entire chain must be removed. resource() in VehicleService auto-reacts on org changes.

Color tokens (from 03-UI-SPEC.md):
  stale: --vd-offline (#5a4530) at opacity 0.5 applied to marker outer div
  running: --vd-healthy (#84cc16)
  alert: --vd-warning (#eab308)  [NOTE: check existing — PATTERNS.md says #ef4444 for alert but UI-SPEC says #eab308 for alert state. UI-SPEC wins.]
  offline: --vd-offline (#5a4530)

Toast copy (from 03-UI-SPEC.md / 03-CONTEXT.md D-20):
  connected→disconnected: "Connection lost — reconnecting…" (severity: 'warning', auto-dismisses at 12000ms)
  disconnected→connected: "Live data restored." (severity: 'info', auto-dismisses at 8000ms)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FleetMapComponent — stale state, fitBounds guard, tooltip, map empty state</name>
  <files>
    packages/dashboard/src/app/features/fleet-map/fleet-map.component.ts
    packages/dashboard/src/app/features/fleet-map/fleet-map.component.html
    packages/dashboard/src/app/features/fleet-map/fleet-map.component.spec.ts
  </files>
  <read_first>
    packages/dashboard/src/app/features/fleet-map/fleet-map.component.ts
    packages/dashboard/src/app/features/fleet-map/fleet-map.component.html
    packages/dashboard/src/app/features/fleet-map/fleet-map.component.spec.ts
  </read_first>
  <behavior>
    After implementation, these tests from the Wave 0 stubs must pass:
    - "getVehicleState: returns 'stale' when telemetry timestamp is 16 minutes old" — engine_on: true is irrelevant when stale
    - "getVehicleState: returns 'running' when telemetry is fresh and engine_on is true"
    - "getVehicleState: stale takes priority over alert — alertCount > 0 but timestamp is 16 min old"
  </behavior>
  <action>
    Read fleet-map.component.ts fully to identify exact line numbers before making changes.

    CHANGE 1 — getVehicleState() return type and body (per D-18, stale priority first):
    Replace the existing 3-state method with 4-state version. Return type: 'stale' | 'offline' | 'running' | 'alert'.
    Implementation order: check timestamp staleness first (> 15 * 60 * 1000 ms from Date.now()), then inactive status, then alertCount > 0, then engine_on, then default offline.
    The variable ts = vehicle.latestTelemetry?.timestamp; if ts is falsy (no telemetry), skip stale check.

    CHANGE 2 — getMarkerIcon() stale branch:
    Add 'stale' to the colors Record: stale: '#5a4530' (same as offline, per UI-SPEC).
    Add opacity logic: const opacity = state === 'stale' ? '0.5' : '1';
    Apply opacity to the outer marker div via inline style: style="opacity:${opacity}".
    No pulsing animation on stale markers (static only — do not add 'stale' to the pulse condition).
    Per UI-SPEC: alert color is #eab308 (warning yellow), not #ef4444. Update the alert entry in colors map if it currently uses #ef4444.

    CHANGE 3 — initialFitDone guard in updateMarkers():
    Add private class field: private initialFitDone = false;
    Locate the fitBounds() call block in updateMarkers(). Wrap the entire fitBounds block with: if (validVehicles.length > 0 && !this.initialFitDone) { ... this.initialFitDone = true; }
    Do not change the fitBounds() logic itself — only add the guard condition.

    CHANGE 4 — Stale tooltip in updateMarkers():
    After setting marker position/icon (both for new marker creation and existing marker update), evaluate state:
    If state === 'stale': compute minsAgo = Math.floor((Date.now() - new Date(vehicle.latestTelemetry!.timestamp).getTime()) / 60000); call marker.bindTooltip(`Last seen ${minsAgo} min ago`, { permanent: false });
    Else: call marker.unbindTooltip();

    CHANGE 5 — Map empty state overlay in HTML template:
    Add an @if block above or overlaying the map div. Condition: vehicleService.vehiclesWithHealth().length === 0 && !vehicleService.vehicleResource.isLoading().
    Show a centered absolute-positioned div with semi-transparent background rgba(14, 10, 7, 0.75), border-radius var(--vd-radius-lg), containing the text "No vehicles with GPS data" using body typography.
    The Leaflet map div itself must always render (Leaflet needs the DOM element on init) — the empty state overlay goes ON TOP via position: absolute, not instead of the map.
    Also add LoadingSpinnerComponent overlay (same pattern) when vehicleService.vehicleResource.isLoading() is true.
    Import LoadingSpinnerComponent in the component's imports[] array if not already present.
  </action>
  <verify>
    <automated>cd packages/dashboard && npm test -- --testPathPattern=fleet-map.component 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm test --testPathPattern=fleet-map.component shows all 3 getVehicleState() tests passing (green)
    - fleet-map.component.ts return type of getVehicleState() is 'stale' | 'offline' | 'running' | 'alert'
    - fleet-map.component.ts contains "private initialFitDone = false" class field
    - fleet-map.component.ts contains "this.initialFitDone = true" inside the fitBounds block
    - fleet-map.component.ts contains "15 * 60 * 1000" stale threshold
    - fleet-map.component.ts contains "bindTooltip" call for stale markers
    - fleet-map.component.html contains empty state conditional block with text "No vehicles with GPS data"
    - fleet-map.component.html contains LoadingSpinnerComponent reference for loading state
    - cd packages/dashboard && npm run build — no TypeScript errors for fleet-map.component.ts
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DashboardComponent + VehicleGridComponent — resource-driven loading/empty states</name>
  <files>
    packages/dashboard/src/app/features/dashboard/dashboard.component.ts
    packages/dashboard/src/app/features/dashboard/dashboard.component.html
    packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-grid.component.ts
    packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-grid.component.html
    packages/dashboard/src/app/features/dashboard/dashboard.component.spec.ts
    packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-card/vehicle-card.component.spec.ts
  </files>
  <read_first>
    packages/dashboard/src/app/features/dashboard/dashboard.component.ts
    packages/dashboard/src/app/features/dashboard/dashboard.component.html
    packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-grid.component.ts
    packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-grid.component.html
    packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-card/vehicle-card.component.ts
    packages/dashboard/src/app/features/dashboard/dashboard.component.spec.ts
    packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-card/vehicle-card.component.spec.ts
    packages/dashboard/src/app/app.routes.ts
  </read_first>
  <behavior>
    After implementation, these Wave 0 stubs must pass:
    - "shows LoadingSpinnerComponent when vehicleResource.isLoading is true"
    - "shows empty state when vehiclesWithHealth is empty array and not loading"
    - "renders healthScore value in the health gauge element"
    - "applies correct state CSS class based on vehicle state"
  </behavior>
  <action>
    Read all four component files fully before touching them. Read app.routes.ts to confirm /vehicle/:id route exists.

    DASHBOARD COMPONENT (dashboard.component.ts):
    Add these signal aliases derived from VehicleService:
      readonly isLoading = this.vehicleService.vehicleResource.isLoading;
      readonly vehiclesWithHealth = this.vehicleService.vehiclesWithHealth;
      readonly hasError = computed(() => this.vehicleService.vehicleResource.status() === 'error');
    Add computed to Angular core imports if not already there.
    Keep existing properties (vehicleCount, etc.) if they still work post-refactor; remove any that reference deleted signals (e.g. vehicleService.isLoading, vehicleService.vehicles — these no longer exist).

    DASHBOARD TEMPLATE (dashboard.component.html):
    Replace the content area (vehicle grid section) with a three-branch @if/@else if/@else block:
      @if (isLoading()) { <app-loading-spinner message="Loading fleet..." [fullPage]="false" /> }
      @else if (vehiclesWithHealth().length === 0) { [empty state block] }
      @else { <app-vehicle-grid /> }
    Empty state block (per D-23 and UI-SPEC copywriting contract):
      Centered div with icon (use mat-icon "directions_car" or similar), heading "No vehicles in your fleet" using --vd-text-xl Space Grotesk 600, body text "Add your first vehicle to start monitoring fleet health." using --vd-text-md Inter 400, and a button "+ Add Vehicle" styled with --vd-brand background, routing to /fleet-management via routerLink="/fleet-management".
      Use Angular Router routerLink directive — import RouterLink in component imports[] if missing.

    VEHICLE GRID COMPONENT (vehicle-grid.component.ts):
    Update signal bindings to use resource signals:
      readonly isLoading = this.vehicleService.vehicleResource.isLoading;   (replaces vehicleService.isLoading)
      readonly hasError = computed(() => this.vehicleService.vehicleResource.status() === 'error');
      readonly errorMessage = this.vehicleService.vehicleResource.error;    (replaces vehicleService.error)
    Vehicles signal stays as: readonly vehicles = this.vehicleService.vehiclesWithHealth;
    Add computed to imports if missing.
    If an error banner exists in the template, update its binding from the old error signal to the new errorMessage signal.

    VEHICLE GRID TEMPLATE (vehicle-grid.component.html):
    If the grid template already handles isLoading/error states, verify they now bind to the updated signals.
    VehicleCardComponent click output: if vehicle-card.component.ts emits a (clicked) output with vehicle or vehicle.id, the grid must handle it with a router.navigate(['/vehicle', vehicle.id]) call. Read vehicle-card.component.ts to confirm the output event name and payload type. Import Router and inject it if not already present in VehicleGridComponent.

    VEHICLE CARD COMPONENT — do NOT modify unless:
    - Card click output exists but is not connected to navigation (wire it)
    - The "View full details" link navigates incorrectly (fix the route to /vehicle/:id per D-03/D-22)
    Keep calculateHealthScore, health gauge, battery, DTC display — no changes to those.
  </action>
  <verify>
    <automated>cd packages/dashboard && npm test -- --testPathPattern="dashboard.component|vehicle-card.component" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm test --testPathPattern="dashboard.component|vehicle-card.component" — all 4 stubs passing (green)
    - dashboard.component.ts contains "vehicleResource.isLoading" property alias
    - dashboard.component.html contains "@if (isLoading())" conditional
    - dashboard.component.html contains text "No vehicles in your fleet" in @else if branch
    - dashboard.component.html contains routerLink="/fleet-management" on Add Vehicle button
    - dashboard.component.html contains "+ Add Vehicle" button text (per UI-SPEC copywriting)
    - vehicle-grid.component.ts no longer references "vehicleService.isLoading" or "vehicleService.error" directly (now uses vehicleResource signals)
    - Vehicle card click navigates to /vehicle/:id (confirmed by test or code inspection of router.navigate call)
    - cd packages/dashboard && npm run build — no TypeScript errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: AlertService + ShellComponent — add pushAlert(), wire ConnectionPill and connection toast</name>
  <files>
    packages/dashboard/src/app/core/services/alert.service.ts
    packages/dashboard/src/app/layout/shell/shell.component.ts
    packages/dashboard/src/app/layout/shell/shell.component.html
  </files>
  <read_first>
    packages/dashboard/src/app/core/services/alert.service.ts
    packages/dashboard/src/app/core/models/alert.model.ts
    packages/dashboard/src/app/layout/shell/shell.component.ts
    packages/dashboard/src/app/layout/shell/shell.component.html
    packages/dashboard/src/app/shared/ui/connection-pill/connection-pill.component.ts
    packages/dashboard/src/app/core/services/telemetry.service.ts
  </read_first>
  <action>
    Read all files listed in read_first fully before making changes.

    CHANGE 1 — Add pushAlert() to AlertService (alert.service.ts):
    AlertService.createAlert() is private. ToastComponent reads from AlertService.activeAlerts() computed signal.
    The only way to display a connection toast is to push an Alert into _alerts via a new public method.

    Add this public method to AlertService after the clearAllAlerts() method (around line 90):

      pushAlert(message: string, severity: AlertSeverity, type: AlertType = 'connection'): void {
        this._alerts.update(alerts => {
          const trimmed = alerts.length >= 200 ? alerts.slice(-199) : alerts;
          const id = `alert-${Date.now()}-${++alertIdCounter}`;
          return [...trimmed, {
            id,
            vehicleId: 'fleet',
            type,
            severity,
            message,
            timestamp: new Date(),
            status: 'active' as AlertStatus,
          }];
        });
      }

    alertIdCounter is the module-level let variable already declared at line 9 of alert.service.ts — increment it here.
    AlertSeverity, AlertType, AlertStatus are already imported from alert.model.ts in the service file.

    CHANGE 2 — Remove ngOnInit manual load chain from ShellComponent:
    Delete the ngOnInit() method body that chains loadOrganizations() → loadFleets() → loadVehicles() → loadInitialTelemetry(). If ngOnInit has no other content, remove the method and remove 'OnInit' from implements clause. resource() in VehicleService auto-loads when selectedOrganization is set.

    CHANGE 3 — Inject TelemetryService and AlertService, expose connectionStatus in ShellComponent:
    Add: private readonly telemetryService = inject(TelemetryService);
    Add (if not already present): private readonly alertService = inject(AlertService);
    Add: readonly connectionStatus = this.telemetryService.connectionStatus;
    Add TelemetryService import from its path if missing.

    CHANGE 4 — Add toast effect for connection status transitions (D-20):
    In the ShellComponent constructor (injection context), add an effect() that watches connectionStatus:

      private previousConnectionStatus: string | null = null;

      // In constructor:
      effect(() => {
        const status = this.telemetryService.connectionStatus();
        if (this.previousConnectionStatus !== null && this.previousConnectionStatus !== status) {
          if (status === 'disconnected') {
            this.alertService.pushAlert('Connection lost — reconnecting…', 'warning');
          } else if (status === 'connected' && this.previousConnectionStatus === 'disconnected') {
            this.alertService.pushAlert('Live data restored.', 'info');
          }
        }
        this.previousConnectionStatus = status;
      });

    Add effect to Angular core imports.
    Note on auto-dismiss timing: ALERT_AUTO_DISMISS.warning = 12000ms (disconnect toast), ALERT_AUTO_DISMISS.info = 8000ms (reconnect toast). UI-SPEC calls for 3000ms reconnect dismiss — 8000ms is the closest available without modifying ALERT_AUTO_DISMISS. Document this deviation in the SUMMARY.

    CHANGE 5 — Add VdConnectionPillComponent to shell template:
    In the shell header template, add <vd-connection-pill [state]="connectionStatus()" /> where other header controls appear (right side of toolbar, per UI-SPEC layout).
    Add VdConnectionPillComponent to the component's imports[] array.
    Add import statement: import { VdConnectionPillComponent } from '../../shared/ui/connection-pill/connection-pill.component';

    CHANGE 6 — aria-label on header icon buttons:
    Per UI-SPEC checker sign-off flag: ensure any mat-icon-button elements in the shell header template have aria-label attributes. Add aria-label="[descriptive label]" to any that are missing it.

    Do NOT remove or change anything in the shell template related to sidebar, nav links, org switcher, or auth state — only modify the header toolbar area and remove the ngOnInit load chain.
  </action>
  <verify>
    <automated>
      cd packages/dashboard && npm run build 2>&1 | grep -E "^ERROR|^.* Error:" | head -20

      Also verify selector appears in template:
      grep -c "vd-connection-pill" packages/dashboard/src/app/layout/shell/shell.component.html
    </automated>
  </verify>
  <acceptance_criteria>
    - alert.service.ts contains "pushAlert(" public method
    - alert.service.ts pushAlert() uses vehicleId: 'fleet' as sentinel
    - shell.component.ts does NOT contain loadVehicles, loadInitialTelemetry, loadFleets, loadOrganizations calls
    - shell.component.ts contains "telemetryService = inject(TelemetryService)"
    - shell.component.ts contains "connectionStatus = this.telemetryService.connectionStatus"
    - shell.component.ts contains "effect(() =>" in the constructor for toast wiring
    - shell.component.ts contains "previousConnectionStatus" private field for transition tracking
    - shell.component.ts contains "alertService.pushAlert(" calls for disconnect and reconnect
    - shell.component.html contains "vd-connection-pill" element (grep -c returns >= 1)
    - shell.component.html contains '[state]="connectionStatus()"' binding
    - VdConnectionPillComponent appears in shell.component.ts imports[] array
    - All mat-icon-button elements in shell.component.html have aria-label attributes
    - cd packages/dashboard && npm run build — no TypeScript errors (grep for ERROR returns empty)
    - git -C packages/dashboard log --oneline -3 shows commits for fleet-map, dashboard, and shell changes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Leaflet DOM | Leaflet renders outside Angular zone; OnPush + effect() drives updates |
| Dashboard → Supabase Realtime | connectionStatus signal reflects WebSocket state; RLS filters events before delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | FleetMapComponent stale markers | accept | Stale markers show last known GPS position — no new data exposed. Staleness is a visual hint, not a security boundary. RLS on telemetry_logs (migration 012) is the actual enforcement. |
| T-03-06 | Denial of Service | Shell ngOnInit load chain removal | accept | Removing the manual load chain means no redundant Supabase queries on every shell mount. resource() is parameterized — no load fires without a valid org. Reduces query surface area. |
| T-03-07 | Information Disclosure | Connection pill WebSocket state | accept | connectionStatus signal only exposes 'connected'/'reconnecting'/'disconnected' — no JWT, no user data, no vehicle data. Safe to display in shell header. |
</threat_model>

<verification>
1. npm test (full suite) from packages/dashboard — all Wave 0 stubs now green
2. npm run build from packages/dashboard — zero TypeScript errors
3. Manual smoke: npm run dev:simulator + npm run dev:parser + npm start (dashboard) → open /map → verify markers appear, connection pill shows 'connected', no console errors
4. Manual: kill parser → connection pill transitions to 'disconnected' → toast "Connection lost — reconnecting…" appears
5. Manual: insert telemetry with timestamp = NOW() - INTERVAL '20 minutes' → open /map → verify grey dimmed marker + "Last seen 20 min ago" tooltip
6. Manual: /dashboard with empty fleet → LoadingSpinner during load → empty state "No vehicles in your fleet" after resolved
7. git -C packages/dashboard log --oneline -5 shows commits for all modified files
</verification>

<success_criteria>
- FleetMapComponent: 4-state getVehicleState() with stale priority, initialFitDone guard, stale tooltip, map empty state overlay
- DashboardComponent: LoadingSpinner while loading, empty state with CTA to /fleet-management when resolved empty
- VehicleGridComponent: binds to vehicleResource signals, no reference to deleted isLoading/error vehicle service signals
- AlertService: public pushAlert() method added; type 'connection' + vehicleId 'fleet' sentinel
- ShellComponent: no manual load chain, VdConnectionPillComponent wired to connectionStatus, toast effect using alertService.pushAlert()
- All 8 Wave 0 unit tests from vehicle.service.spec.ts passing (green)
- All 3 fleet-map.component.spec.ts stale state tests passing (green)
- All 4 dashboard + vehicle-card spec stubs passing (green)
- Full test suite green: npm test from packages/dashboard
- TypeScript build passes: npm run build from packages/dashboard
- FLEET-01, FLEET-02, FLEET-03 all addressed across plans 03-01 and 03-02
</success_criteria>

<output>
After completion, create .planning/phases/03-live-fleet-dashboard/03-02-SUMMARY.md with:
- FleetMapComponent changes: exact methods modified, test results
- DashboardComponent + VehicleGridComponent changes: what states work, test results
- AlertService: pushAlert() method added (note deviation: reconnect toast auto-dismisses at 8000ms, not 3000ms as UI-SPEC specified — ALERT_AUTO_DISMISS.info is 8000ms)
- ShellComponent: what load chain was removed, how toast was wired
- Full test suite result (pass/fail count)
- Build status
- Any deviations from plan
- Manual smoke test results (if performed)
</output>
