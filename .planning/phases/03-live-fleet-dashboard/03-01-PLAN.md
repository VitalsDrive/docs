---
phase: 03-live-fleet-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/supabase/migrations/012_telemetry_rls_and_rpc.sql
  - packages/dashboard/src/app/core/services/vehicle.service.ts
  - packages/dashboard/src/app/core/services/vehicle.service.spec.ts
  - packages/dashboard/src/app/features/fleet-map/fleet-map.component.spec.ts
autonomous: false
requirements:
  - FLEET-01
  - FLEET-02

must_haves:
  truths:
    - "Auth0 users can read telemetry for vehicles in their fleet (RLS uses auth.jwt()->>'sub')"
    - "Parser service-role inserts succeed regardless of RLS"
    - "get_latest_telemetry(vehicle_ids) RPC returns exactly one row per vehicle"
    - "VehicleService loads vehicles reactively via resource() when selectedOrganization changes"
    - "Realtime subscription starts (via effect) only after vehicleResource resolves"
    - "Telemetry snapshot seeds telemetryMap on initial load via telemetryResource"
    - "reloadVehicles$.next() cancels prior telemetryBatch$ subscription on org switch"
    - "Wave 0 test stubs exist and fail (red) before Wave 1 implementation"
  artifacts:
    - path: "packages/supabase/migrations/012_telemetry_rls_and_rpc.sql"
      provides: "RLS policy fix + get_latest_telemetry RPC"
      contains: "auth.jwt()->>'sub'"
    - path: "packages/dashboard/src/app/core/services/vehicle.service.ts"
      provides: "resource()-driven vehicle + telemetry loading"
      exports: ["vehicleResource", "telemetryResource", "vehiclesWithHealth"]
    - path: "packages/dashboard/src/app/core/services/vehicle.service.spec.ts"
      provides: "Unit tests for resource() reactive chain"
    - path: "packages/dashboard/src/app/features/fleet-map/fleet-map.component.spec.ts"
      provides: "Unit tests for getVehicleState() stale logic"
  key_links:
    - from: "VehicleService.vehicleResource.params"
      to: "OrganizationService.selectedOrganization()"
      via: "computed params lambda"
      pattern: "selectedOrganization\\(\\)"
    - from: "VehicleService constructor effect()"
      to: "TelemetryService.subscribeToFleet()"
      via: "vehicleResource.status() === 'resolved'"
      pattern: "vehicleResource\\.status\\(\\).*resolved"
    - from: "telemetry_logs RLS policy"
      to: "fleet_members.user_id"
      via: "auth.jwt()->>'sub'"
      pattern: "auth\\.jwt\\(\\)->>'sub'"
---

<objective>
Wire the database security layer and the Angular reactive data bootstrap chain.

Purpose: The existing telemetry_logs RLS policy uses auth.uid() which is broken for Auth0 users. VehicleService currently uses imperative loadVehicles() called from ShellComponent ngOnInit — this must become a resource()-driven reactive chain that auto-reloads on org change. This plan fixes both the security blocker and the data bootstrap.

Output:
- Migration 012: new telemetry_logs RLS policy (auth.jwt()->>'sub') + get_latest_telemetry() RPC
- VehicleService: vehicleResource + telemetryResource + effect() bridge replacing loadVehicles()/loadInitialTelemetry()
- Wave 0 test stubs (red) for vehicle.service and fleet-map.component
</objective>

<execution_context>
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-01-PLAN.md
</execution_context>

<context>
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/ROADMAP.md
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-CONTEXT.md
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-RESEARCH.md
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-PATTERNS.md
@C:/Users/ronbiter/Documents/projects/VitalsDrive/.planning/phases/03-live-fleet-dashboard/03-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Read these files before touching any modified file. -->

From packages/supabase/migrations/010_fix_rls_policies.sql (analog for migration 012):
  Pattern: BEGIN; DROP POLICY IF EXISTS "name" ON table; CREATE POLICY "name" ON table FOR SELECT USING (... auth.jwt()->>'sub' ...); COMMIT;

From packages/supabase/migrations/002_rls_policies.sql:
  Existing broken policy name: "Users can read own fleet telemetry" ON telemetry_logs
  auth.uid() usage — this is what migration 012 must DROP and replace.

From packages/dashboard/src/app/core/services/vehicle.service.ts (to be refactored):
  Existing signals: vehicles = signal<Vehicle[]>([]), isLoading = signal(false), error = signal<string|null>(null)
  Methods to DELETE: loadVehicles(), loadInitialTelemetry()
  Methods to KEEP: calculateHealthScore(), mapDbRecord(), processBatch(), createVehicle(), updateVehicle(), deleteVehicle(), ngOnDestroy()
  Existing subjects to KEEP: destroy$ = new Subject<void>(), reloadVehicles$ = new Subject<void>()
  Existing injections to KEEP: supabase, telemetryService, organizationService, fleetService, alertService

From packages/dashboard/src/app/core/services/organization.service.ts (resource() pattern reference):
  inject pattern: private readonly organizationService = inject(OrganizationService)
  Signal: selectedOrganization — read via selectedOrganization()

From packages/dashboard/src/app/core/services/fleet.service.ts:
  Signal: fleets() — returns Fleet[]; each fleet has organization_id: string and id: string

From packages/dashboard/src/app/core/services/telemetry.service.ts:
  Methods: subscribeToFleet() — starts fleet-wide postgres_changes channel
  Observables: telemetryBatch$ — RxJS Subject<TelemetryRecord[]> emitting 500ms-buffered batches
  Signal: connectionStatus — signal<ConnectionState>

Angular resource() API (stable, Angular 19+):
  import { resource, effect } from '@angular/core'
  resource({ params: () => R | undefined, loader: ({ params }) => Promise<T>, defaultValue?: T })
  NOTE: loader receives { params, abortSignal } but .abortSignal() is NOT available on supabase-js
  query builder (verified v2.104.1). Do NOT call .abortSignal() — omit it entirely.
  ResourceRef: .value() Signal<T|undefined>, .status() Signal<'idle'|'loading'|'refreshing'|'resolved'|'error'|'local'>, .isLoading Signal<boolean>, .error Signal<unknown>
  params returning undefined → loader skips, status = 'idle'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Write failing test stubs for resource() chain and stale state</name>
  <files>
    packages/dashboard/src/app/core/services/vehicle.service.spec.ts
    packages/dashboard/src/app/features/fleet-map/fleet-map.component.spec.ts
    packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-card/vehicle-card.component.spec.ts
    packages/dashboard/src/app/features/dashboard/dashboard.component.spec.ts
  </files>
  <read_first>
    packages/dashboard/src/app/core/services/vehicle.service.ts
    packages/dashboard/src/app/features/fleet-map/fleet-map.component.ts
    packages/dashboard/src/app/features/dashboard/vehicle-grid/vehicle-card/vehicle-card.component.ts
    packages/dashboard/src/app/features/dashboard/dashboard.component.ts
    packages/dashboard/jest.config.js
  </read_first>
  <behavior>
    vehicle.service.spec.ts stubs (all must FAIL before Task 2 implementation):
    - "vehicleResource: skips loader when selectedOrganization is null" — params returns undefined, status === 'idle'
    - "vehicleResource: reloads vehicles when selectedOrganization changes" — fakeAsync, set org1 → tick → expect vehicles from org1; set org2 → tick → expect vehicles from org2
    - "vehicleResource: loader queries vehicles WHERE fleet_id IN org's fleet IDs AND status = 'active'"
    - "effect: calls subscribeToFleet() when vehicleResource status transitions to resolved"
    - "effect: calls reloadVehicles$.next() before wiring telemetryBatch$ subscription on reload"
    - "telemetryResource: skips loader when vehicleIds is empty array" — params returns undefined, status === 'idle'
    - "telemetryResource: calls get_latest_telemetry RPC with vehicle IDs array"
    - "telemetryResource: seeds telemetryMap with snapshot grouped by vehicle_id"

    fleet-map.component.spec.ts stubs (all must FAIL before Task 3 implementation):
    - "getVehicleState: returns 'stale' when telemetry timestamp is 16 minutes old" — engine_on: true ignored
    - "getVehicleState: returns 'running' when telemetry is fresh and engine_on is true"
    - "getVehicleState: stale takes priority over alert — alertCount > 0 but timestamp is 16 min old"

    vehicle-card.component.spec.ts stub:
    - "renders healthScore value in the health gauge element"
    - "applies correct state CSS class based on vehicle state"

    dashboard.component.spec.ts stub:
    - "shows LoadingSpinnerComponent when vehicleResource.isLoading is true"
    - "shows empty state when vehiclesWithHealth is empty array and not loading"
  </behavior>
  <action>
    Create the four spec files. Use Angular TestBed with provideExperimentalZonelessChangeDetection() for component specs (Angular 21 pattern). For vehicle.service.spec.ts, use TestBed.inject(VehicleService) after providing mock OrganizationService, FleetService, SupabaseService, TelemetryService, AlertService via jasmine spyObj or jest.fn(). Mock SupabaseService.client with a jest.fn() object returning { data: [], error: null }. All test bodies must call expect(...).toBe(...) with the target value — write the assertion first, then leave implementation empty so tests fail. Do NOT import from files that do not yet export the new resource() properties (tests will fail at import or at assertion — both are valid red states). Use describe/it blocks. Include beforeEach with TestBed.configureTestingModule. No watch flags. Jest timeout stays default (5000ms). Commit these stubs with message: "test(03-wave0): add failing stubs for resource() chain and stale state".
  </action>
  <verify>
    <automated>cd packages/dashboard && npm test -- --testPathPattern="vehicle.service|fleet-map.component|vehicle-card.component|dashboard.component" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All four spec files exist under their respective component/service directories
    - npm test output shows tests FAILING (red) — not erroring with "cannot find module" for stable imports
    - vehicle.service.spec.ts contains at least 8 it() blocks covering the behaviors listed above
    - fleet-map.component.spec.ts contains at least 3 it() blocks for getVehicleState() stale priority
    - No spec file uses --watch or --watchAll flags
    - Commit hash exists for the wave 0 stubs (git -C packages/dashboard log --oneline -1 shows the test commit)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migration 012 SQL file is ready to be deployed. Before applying, the parser's service role key must be confirmed — otherwise enabling stricter RLS could break telemetry inserts.</what-built>
  <how-to-verify>
    Confirm the following before proceeding:
    1. Open packages/parser/.env (or packages/parser/src/config.ts) and confirm SUPABASE_SERVICE_ROLE_KEY is set (not anon key). The service role key starts with "eyJ" and is longer than the anon key.
    2. If using Railway: confirm the SUPABASE_SERVICE_ROLE_KEY env var is set in the Railway dashboard for the parser service.
    3. Type "confirmed" if the service role key is in place, or describe what you see.
  </how-to-verify>
  <resume-signal>Type "confirmed" to proceed to migration deployment, or describe the parser key situation.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Migration 012 — Fix telemetry_logs RLS + create get_latest_telemetry RPC</name>
  <files>
    packages/supabase/migrations/012_telemetry_rls_and_rpc.sql
  </files>
  <read_first>
    packages/supabase/migrations/010_fix_rls_policies.sql
    packages/supabase/migrations/002_rls_policies.sql
    packages/supabase/migrations/003_functions_triggers.sql
  </read_first>
  <action>
    Create packages/supabase/migrations/012_telemetry_rls_and_rpc.sql. Structure exactly mirrors 010_fix_rls_policies.sql: BEGIN/COMMIT wrapping, DROP POLICY IF EXISTS before CREATE POLICY, comment block at top explaining what is being fixed and why.

    Migration must do three things in this order:

    1. Drop the existing broken policy: DROP POLICY IF EXISTS "Users can read own fleet telemetry" ON telemetry_logs;

    2. Create new policy using auth.jwt()->>'sub' (same join pattern as the vehicles policy in migration 010):
       CREATE POLICY "Users can read own fleet telemetry" ON telemetry_logs FOR SELECT USING (EXISTS (SELECT 1 FROM vehicles v JOIN fleet_members fm ON fm.fleet_id = v.fleet_id WHERE v.id = telemetry_logs.vehicle_id AND fm.user_id = (auth.jwt()->>'sub')));

    3. Create the RPC function:
       CREATE OR REPLACE FUNCTION get_latest_telemetry(vehicle_ids uuid[]) RETURNS SETOF telemetry_logs LANGUAGE sql SECURITY INVOKER STABLE AS $$ SELECT DISTINCT ON (vehicle_id) * FROM telemetry_logs WHERE vehicle_id = ANY(vehicle_ids) ORDER BY vehicle_id, timestamp DESC; $$;

    SECURITY INVOKER (not DEFINER) is mandatory per D-11 — the function runs as the calling user so RLS on telemetry_logs applies automatically.
    Do NOT touch the existing get_latest_telemetry_per_vehicle(p_fleet_id UUID) function from migration 003 — different function name, different signature, leave it untouched.
    Do NOT add INSERT or UPDATE policies — parser uses service role key which bypasses RLS entirely.

    After writing the file, deploy to production:
      cd packages/supabase && supabase db push --project-ref odwctmlawibhaclptsew
  </action>
  <verify>
    <automated>
      Use mcp__claude_ai_Supabase__execute_sql with project_id "odwctmlawibhaclptsew" to run these two queries after supabase db push completes:

      Query 1 — verify RLS policy exists:
        SELECT count(*) AS policy_count FROM pg_policies WHERE tablename = 'telemetry_logs' AND policyname = 'Users can read own fleet telemetry';
      Expected: policy_count = 1

      Query 2 — verify RPC function exists:
        SELECT count(*) AS fn_count FROM pg_proc WHERE proname = 'get_latest_telemetry';
      Expected: fn_count = 1 (the new function; get_latest_telemetry_per_vehicle is a different name and does not match)

      Both queries must return expected counts before marking this task complete.
    </automated>
  </verify>
  <acceptance_criteria>
    - File packages/supabase/migrations/012_telemetry_rls_and_rpc.sql exists
    - File contains "auth.jwt()->>'sub'" (not "auth.uid()")
    - File contains "SECURITY INVOKER" (not "SECURITY DEFINER")
    - File contains "DISTINCT ON (vehicle_id)" in the RPC body
    - File contains "RETURNS SETOF telemetry_logs" (full row — not partial columns)
    - supabase db push succeeds (exit 0) against project odwctmlawibhaclptsew
    - pg_policies query returns count = 1 for telemetry_logs named "Users can read own fleet telemetry"
    - pg_proc query returns count = 1 for get_latest_telemetry (new function exists)
    - git -C packages/supabase log --oneline -1 shows a commit for migration 012
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VehicleService — Replace loadVehicles/loadInitialTelemetry with resource() + effect() bridge</name>
  <files>
    packages/dashboard/src/app/core/services/vehicle.service.ts
    packages/dashboard/src/app/core/services/vehicle.service.spec.ts
  </files>
  <read_first>
    packages/dashboard/src/app/core/services/vehicle.service.ts
    packages/dashboard/src/app/core/services/organization.service.ts
    packages/dashboard/src/app/core/services/fleet.service.ts
    packages/dashboard/src/app/core/services/telemetry.service.ts
    packages/dashboard/src/app/core/models/vehicle.model.ts
    packages/dashboard/src/app/core/services/vehicle.service.spec.ts
  </read_first>
  <behavior>
    After implementation, these tests from Task 1 must pass (green):
    - "vehicleResource: skips loader when selectedOrganization is null"
    - "vehicleResource: reloads vehicles when selectedOrganization changes"
    - "vehicleResource: loader queries vehicles WHERE fleet_id IN org's fleet IDs AND status = 'active'"
    - "effect: calls subscribeToFleet() when vehicleResource status transitions to resolved"
    - "effect: calls reloadVehicles$.next() before wiring telemetryBatch$ subscription on reload"
    - "telemetryResource: skips loader when vehicleIds is empty array"
    - "telemetryResource: calls get_latest_telemetry RPC with vehicle IDs array"
    - "telemetryResource: seeds telemetryMap with snapshot grouped by vehicle_id"
  </behavior>
  <action>
    Read vehicle.service.ts fully before making changes. Then apply these precise modifications:

    IMPORTS — add to existing import from '@angular/core':
      resource, effect (alongside existing: Injectable, computed, inject, signal, OnDestroy)

    ADD class fields (field initializer position = injection context — place after existing inject() declarations):

    readonly vehicleResource = resource({
      params: () => {
        const org = this.organizationService.selectedOrganization();
        if (!org) return undefined;
        const fleetIds = this.fleetService.fleets()
          .filter(f => f.organization_id === org.id)
          .map(f => f.id);
        if (fleetIds.length === 0) return undefined;
        return { fleetIds };
      },
      loader: async ({ params: { fleetIds } }) => {
        // NOTE: .abortSignal() is NOT available on supabase-js v2.104.1 query builder — omit it
        const { data, error } = await this.supabase.client
          .from('vehicles')
          .select('*')
          .in('fleet_id', fleetIds)
          .eq('status', 'active')
          .order('make');
        if (error) throw error;
        return (data ?? []) as Vehicle[];
      },
      defaultValue: [],
    });

    private readonly vehicleIds = computed(() =>
      this.vehicleResource.value()?.map(v => v.id) ?? []
    );

    readonly telemetryResource = resource({
      params: () => {
        const ids = this.vehicleIds();
        return ids.length > 0 ? ids : undefined;
      },
      loader: async ({ params: vehicleIds }) => {
        const { data, error } = await this.supabase.client
          .rpc('get_latest_telemetry', { vehicle_ids: vehicleIds });
        if (error) throw error;
        const grouped = new Map<string, TelemetryRecord[]>();
        for (const record of (data ?? [])) {
          grouped.set(record.vehicle_id, [this.mapDbRecord(record)]);
        }
        this.telemetryMap.set(grouped);
        return data;
      },
    });

    UPDATE constructor — ADD effect() call inside constructor body (injection context):
      effect(() => {
        if (this.vehicleResource.status() === 'resolved') {
          this.reloadVehicles$.next();
          this.telemetryService.subscribeToFleet();
          this.telemetryService.telemetryBatch$
            .pipe(takeUntil(this.reloadVehicles$), takeUntil(this.destroy$))
            .subscribe(batch => this.processBatch(batch));
        }
      });

    UPDATE vehiclesWithHealth computed — replace this.vehicles() with this.vehicleResource.value() ?? []:
      readonly vehiclesWithHealth = computed<VehicleWithHealth[]>(() => {
        const map = this.telemetryMap();
        return (this.vehicleResource.value() ?? []).map((v) => {
          const history = map.get(v.id) ?? [];
          const latest = history[0];
          return {
            ...v,
            latestTelemetry: latest,
            healthScore: this.calculateHealthScore(latest),
            alertCount: this.alertService.activeAlerts().filter(a => a.vehicleId === v.id).length,
          };
        });
      });

    DELETE: loadVehicles() method, loadInitialTelemetry() method, isLoading signal, vehicles signal.
    KEEP: calculateHealthScore(), mapDbRecord(), processBatch(), createVehicle(), updateVehicle(), deleteVehicle(), telemetryMap, selectedVehicleId, ngOnDestroy(), destroy$, reloadVehicles$.
  </action>
  <verify>
    <automated>cd packages/dashboard && npm test -- --testPathPattern=vehicle.service 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm test --testPathPattern=vehicle.service shows all 8 vehicle.service tests passing (green)
    - vehicle.service.ts no longer contains loadVehicles or loadInitialTelemetry method names
    - vehicle.service.ts contains "vehicleResource = resource(" as a class field
    - vehicle.service.ts contains "telemetryResource = resource(" as a class field
    - vehicle.service.ts contains "effect(() =>" in the constructor body
    - vehicle.service.ts contains "reloadVehicles$.next()" inside the effect block
    - vehicle.service.ts contains "subscribeToFleet()" inside the effect block
    - vehicle.service.ts does NOT contain ".abortSignal(" (not available in supabase-js v2.104.1)
    - vehiclesWithHealth computed uses "vehicleResource.value() ?? []" (not this.vehicles())
    - cd packages/dashboard && npm run build produces no TypeScript errors for vehicle.service.ts
    - git -C packages/dashboard log --oneline -1 shows a commit for vehicle service refactor
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard client → Supabase | JWT passed in Authorization header; RLS evaluated server-side |
| Supabase Realtime → Dashboard | postgres_changes events filtered by RLS before broadcast |
| Parser → Supabase | Uses SUPABASE_SERVICE_ROLE_KEY; bypasses RLS entirely (write-only path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | telemetry_logs SELECT | mitigate | Migration 012 replaces auth.uid() policy with auth.jwt()->>'sub' JOIN through fleet_members — enforces fleet membership check |
| T-03-02 | Elevation of Privilege | get_latest_telemetry() RPC | mitigate | SECURITY INVOKER (not DEFINER) — RPC runs as calling user; RLS on telemetry_logs applies automatically |
| T-03-03 | Information Disclosure | Supabase Realtime postgres_changes | accept | Realtime RLS check fires before broadcast; once D-12 policy is applied, cross-fleet events are silently dropped — no client-side filtering needed |
| T-03-04 | Denial of Service | Parser inserts post-RLS | accept | Parser uses service role key (bypasses RLS) — INSERT path unaffected by new SELECT policy. Verified by human checkpoint before migration deploy. |
</threat_model>

<verification>
1. Supabase SQL (via mcp__claude_ai_Supabase__execute_sql): SELECT count(*) FROM pg_policies WHERE tablename = 'telemetry_logs' AND policyname = 'Users can read own fleet telemetry' — returns 1
2. Supabase SQL (via mcp__claude_ai_Supabase__execute_sql): SELECT count(*) FROM pg_proc WHERE proname = 'get_latest_telemetry' — returns 1
3. npm test --testPathPattern=vehicle.service — all 8 tests green
4. npm test --testPathPattern=fleet-map.component — stubs exist and fail (red — expected at this wave)
5. cd packages/dashboard && npm run build — no TypeScript errors
</verification>

<success_criteria>
- Migration 012 deployed to project odwctmlawibhaclptsew — RLS uses auth.jwt()->>'sub', old auth.uid() policy dropped
- get_latest_telemetry(vehicle_ids uuid[]) RPC exists with SECURITY INVOKER and DISTINCT ON
- VehicleService has vehicleResource (resource()) + telemetryResource (resource()) + effect() bridge — no loadVehicles() or loadInitialTelemetry()
- vehiclesWithHealth computed reads from vehicleResource.value() not deleted vehicles signal
- vehicle.service.ts does NOT call .abortSignal() on any supabase query builder chain
- Wave 0 test stubs committed (red); vehicle.service tests passing (green)
- TypeScript build passes for dashboard package
- .planning/phases/03-live-fleet-dashboard/03-01-SUMMARY.md exists in phase directory after plan completes
</success_criteria>

<output>
After completion, create .planning/phases/03-live-fleet-dashboard/03-01-SUMMARY.md with:
- Migration 012 deployment status and exact SQL applied
- VehicleService: list of deleted methods, added fields, constructor changes
- Wave 0 test files created and their test counts
- Any deviations from plan (e.g. abortSignal not available in supabase v2.104.1 — already expected)
- Build status
</output>
