---
phase: 02-auth-fleet-management
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/dashboard/src/environments/environment.ts
  - packages/dashboard/src/environments/environment.development.ts
  - packages/dashboard/src/app/core/services/auth.service.ts
  - packages/dashboard/src/app/core/interceptors/auth.interceptor.ts
  - packages/dashboard/src/app/core/guards/auth.guard.ts
  - packages/dashboard/src/app/core/services/auth.service.spec.ts
  - packages/dashboard/src/app/core/guards/auth.guard.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-04

must_haves:
  truths:
    - "auth.isAuthenticated() returns boolean (not Promise) — guards work correctly"
    - "Internal JWT and refresh token stored in localStorage keys vd_access_token and vd_refresh_token"
    - "Every HttpClient request includes Authorization: Bearer <token> header"
    - "On 401 response, interceptor calls /auth/refresh once and retries original request"
    - "On failed refresh, interceptor calls authService.signOut() and throws"
    - "authGuard redirects unauthenticated users to /login"
    - "allowlistGuard removed from auth.guard.ts and all usages in app.routes.ts replaced with onboardingGuard (D-13)"
    - "No hardcoded localhost:3001 — all auth-service calls use environment.authServiceUrl"
    - "AUTH0_CLIENT_ID wired via environment.auth0.clientId in both environment files"
  artifacts:
    - path: "packages/dashboard/src/app/core/services/auth.service.ts"
      provides: "Fixed isAuthenticated signal, localStorage persistence, real exchangeToken, refreshTokens, initializeUserState"
      contains: "toSignal(this.auth0.isAuthenticated$"
    - path: "packages/dashboard/src/app/core/interceptors/auth.interceptor.ts"
      provides: "Token injection + 401 retry with refresh"
      contains: "catchError"
    - path: "packages/dashboard/src/app/core/services/auth.service.spec.ts"
      provides: "Unit tests: isAuthenticated returns boolean, guard redirect"
  key_links:
    - from: "auth.interceptor.ts"
      to: "authService.refreshTokens()"
      via: "catchError on 401, skip if /auth/refresh in URL"
      pattern: "refreshTokens"
    - from: "auth.service.ts exchangeToken()"
      to: "environment.authServiceUrl"
      via: "HttpClient.post"
      pattern: "environment.authServiceUrl"
    - from: "auth.service.ts initializeUserState()"
      to: "supabase.client.from('fleet_members')"
      via: "select organization_id, role"
      pattern: "fleet_members"
---

<objective>
Fix the five critical bugs in the Angular dashboard's auth layer: broken isAuthenticated signal, mocked initializeUserState, hardcoded localhost URLs, missing interceptor 401 retry, and the allowlistGuard that must be removed. Wire localStorage persistence and real Supabase session initialization.

Purpose: AUTH-01 (login redirects work), AUTH-02 (session persists), AUTH-04 (guards protect routes). None of these work today — isAuthenticated returns a Promise which is always truthy, making authGuard pass for everyone.

Output: Working Angular auth with unit tests passing in packages/dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth-fleet-management/02-CONTEXT.md
@.planning/phases/02-auth-fleet-management/02-RESEARCH.md
@.planning/phases/02-auth-fleet-management/02-PATTERNS.md
@.planning/phases/02-auth-fleet-management/02-01-SUMMARY.md

<interfaces>
From packages/dashboard/src/app/core/services/auth.service.ts (current broken state — read the file):
- isAuthenticated: computed(() => firstValueFrom(...)) — BROKEN, returns Promise
- internalToken: private field (may be signal or plain property — read file)
- exchangeToken(auth0Token): calls fetch('http://localhost:3001/auth/exchange') — BROKEN
- initializeUserState(): returns hardcoded true — BROKEN
- signOut(): does not clear localStorage — BROKEN
- checkUserRoles(): hardcodes isAdmin=true, isOwner=true — BROKEN

From packages/dashboard/src/app/core/interceptors/auth.interceptor.ts (current state):
- intercept(): injects Bearer token but has NO 401 retry — BROKEN

From packages/dashboard/src/app/core/guards/auth.guard.ts (current state):
- authGuard, authGuardChild, onboardingGuard, onboardingStepGuard — keep all four
- allowlistGuard — REMOVE entirely (D-13)

Environment files to ADD fields to:
- packages/dashboard/src/environments/environment.ts (production)
- packages/dashboard/src/environments/environment.development.ts (dev)
Add: authServiceUrl: string, auth0: { domain, clientId, audience }

Supabase query for initializeUserState (D-10, D-11):
- Query fleet_members for membership with organization_id
- Query fleets count for the organization_id
- isOnboardingComplete = hasOrg AND hasFleet
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for isAuthenticated signal and authGuard redirect (Wave 0 / RED)</name>
  <files>
    packages/dashboard/src/app/core/services/auth.service.spec.ts
    packages/dashboard/src/app/core/guards/auth.guard.spec.ts
  </files>
  <read_first>
    - packages/dashboard/src/app/core/services/auth.service.ts — read full file: class structure, all signal declarations, auth0 injection, existing imports
    - packages/dashboard/src/app/core/guards/auth.guard.ts — read full file: authGuard function, onboardingGuard function, how they call auth.isAuthenticated() and auth.isLoading()
    - packages/dashboard/karma.conf.js — confirm Karma/Jasmine test setup
    - packages/dashboard/src/app/app.config.ts or app.module.ts — how AuthService and Auth0Module are provided
  </read_first>
  <behavior>
    auth.service.spec.ts:
    - Test 1: isAuthenticated is a Signal (has .apply or call signature indicating it's a Signal, not a Promise — use typeof check: auth.isAuthenticated() must equal false when Auth0 isAuthenticated$ emits false)
    - Test 2: after exchangeToken succeeds, localStorage.getItem('vd_access_token') is set to the returned accessToken
    - Test 3: signOut() clears localStorage keys vd_access_token and vd_refresh_token

    auth.guard.spec.ts:
    - Test 4: authGuard returns UrlTree to '/login' when isAuthenticated() returns false and isLoading() returns false
    - Test 5: authGuard returns true when isAuthenticated() returns true
  </behavior>
  <action>
    Create auth.service.spec.ts using Angular TestBed. Mock AuthService as Auth0Service with isAuthenticated$ BehaviorSubject. Mock HttpClient with HttpClientTestingModule. Spy on localStorage.setItem and localStorage.getItem using jasmine spies or by direct manipulation.

    Create auth.guard.spec.ts using Angular TestBed. Mock AuthService with isAuthenticated signal returning false, then true. Mock Router.createUrlTree. Call authGuard function directly (it is a functional guard exported from auth.guard.ts).

    Run tests — they MUST fail (RED) since the implementation is broken.
  </action>
  <verify>
    <automated>cd packages/dashboard && npm run test -- --include="**/auth.service.spec.ts" --watch=false 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - auth.service.spec.ts exists with 3 tests
    - auth.guard.spec.ts exists with 2 tests
    - Tests run (even if failing due to broken implementation)
    - No compilation errors in spec files themselves (type errors in the service under test are expected)
  </acceptance_criteria>
  <done>5 failing tests written. RED phase complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix environment files, auth.service.ts, auth.interceptor.ts, auth.guard.ts (GREEN)</name>
  <files>
    packages/dashboard/src/environments/environment.ts
    packages/dashboard/src/environments/environment.development.ts
    packages/dashboard/src/app/core/services/auth.service.ts
    packages/dashboard/src/app/core/interceptors/auth.interceptor.ts
    packages/dashboard/src/app/core/guards/auth.guard.ts
    packages/dashboard/src/app/app.routes.ts
  </files>
  <read_first>
    - packages/dashboard/src/environments/environment.ts — read current structure before adding fields
    - packages/dashboard/src/environments/environment.development.ts — read current structure before adding fields
    - packages/dashboard/src/app/core/services/auth.service.ts — read full file again (needs edits in multiple sections)
    - packages/dashboard/src/app/core/interceptors/auth.interceptor.ts — read full file
    - packages/dashboard/src/app/core/guards/auth.guard.ts — read full file, identify allowlistGuard block and line numbers
    - packages/dashboard/src/app/app.routes.ts — read to find all allowlistGuard usages (PATTERNS.md: lines 81, 90, 99, 108, 113)
    - packages/dashboard/src/app/core/services/organization.service.ts — read loadOrganizations() to understand fleet_members query pattern to copy
    - packages/dashboard/src/app/core/services/supabase.service.ts — read client property access pattern
  </read_first>
  <behavior>
    - isAuthenticated returns false when auth0 emits false
    - localStorage stores tokens after exchange
    - interceptor retries on 401
    - authGuard redirects when isAuthenticated() === false
  </behavior>
  <action>
    Fix SIX files in this order:

    1. environment files — add authServiceUrl and auth0 block to BOTH files (D-08, D-09):
    environment.development.ts: authServiceUrl: 'http://localhost:3001', auth0: { domain: 'ronbiter.auth0.com', clientId: 'YOUR_AUTH0_SPA_CLIENT_ID', audience: 'https://ronbiter.auth0.com/api/v2/' }
    environment.ts (production): authServiceUrl: 'https://YOUR_RAILWAY_AUTH_SERVICE_URL', auth0: { domain: 'ronbiter.auth0.com', clientId: 'YOUR_AUTH0_SPA_CLIENT_ID', audience: 'https://ronbiter.auth0.com/api/v2/' }
    Note: clientId is a placeholder — the actual value is loaded from Auth0 dashboard at runtime or set via environment injection. Leave as 'YOUR_AUTH0_SPA_CLIENT_ID' string placeholder.

    2. auth.service.ts — make ALL of these changes:
    a) Fix isAuthenticated signal (CRITICAL — RESEARCH Pitfall 1): Add import { toSignal } from '@angular/core/rxjs-interop'; Remove the computed(() => firstValueFrom(this.auth0.isAuthenticated$)) block. Replace with: readonly isAuthenticated = toSignal(this.auth0.isAuthenticated$, { initialValue: false });
    b) Add localStorage constants as private class fields: private readonly TOKEN_KEY = 'vd_access_token'; private readonly REFRESH_KEY = 'vd_refresh_token';
    c) In constructor: read from localStorage on init: this.internalToken = localStorage.getItem(this.TOKEN_KEY); (or use whatever private field stores the token — read the file to find its name)
    d) Fix exchangeToken(): remove fetch('http://localhost:3001/...') — replace with HttpClient.post using environment.authServiceUrl. After success: localStorage.setItem(this.TOKEN_KEY, data.accessToken); localStorage.setItem(this.REFRESH_KEY, data.refreshToken); and call supabaseService.client.auth.setSession({ access_token: auth0AccessToken, refresh_token: '' }) to enable RLS (D-12). Store the auth0 token separately for Supabase session.
    e) Add refreshTokens() method (needed by interceptor): reads REFRESH_KEY from localStorage, calls HttpClient.post to /auth/refresh, stores new accessToken in TOKEN_KEY.
    f) Add getInternalToken() method if not present: returns this.internalToken (the stored JWT).
    g) Fix initializeUserState() (D-10, D-11): remove all hardcoded true values. Query supabase.client.from('fleet_members').select('organization_id, role').limit(1).single(). If membership exists: set organizationId signal to membership.organization_id, then query fleets count for that org. Set isOnboardingComplete to hasOrg && hasFleet. Copy query pattern from organization.service.ts / fleet.service.ts.
    h) Fix signOut(): add localStorage.removeItem(this.TOKEN_KEY); localStorage.removeItem(this.REFRESH_KEY);
    i) Fix checkUserRoles(): remove hardcoded isAdmin=true, isOwner=true. Read from the currentUser signal's roles array. isOwner = currentUser?.roles.includes('owner') ?? false.
    j) All auth-service HTTP calls: replace any remaining 'http://localhost:3001' strings with `${environment.authServiceUrl}` (RESEARCH Pitfall 4 — 3 occurrences total).
    k) Wire /auth/me validation on app init (D-07): At the START of `initializeUserState()`, read the stored token from localStorage (TOKEN_KEY). If no token: return early (unauthenticated). If token exists: call HttpClient.get(`${environment.authServiceUrl}/auth/me`, { headers: { Authorization: `Bearer ${storedToken}` } }) and pipe catchError. On 401 or error: call signOut() to clear localStorage and return — stop initialization. On success: update the currentUser signal with the returned { sub, email }. This validates the stored JWT against the auth-service before any Supabase queries run.

    3. auth.interceptor.ts — add 401 retry (D-02, D-03):
    Add imports: from 'rxjs' import { from, throwError }; from 'rxjs/operators' import { catchError, switchMap }; HttpErrorResponse from '@angular/common/http'.
    Replace intercept() body with the full pattern from PATTERNS.md (RESEARCH Pattern 3): inject token, if 401 and not /auth/refresh URL → call authService.refreshTokens(), retry with new token, on refresh failure call authService.signOut() and throwError.

    4. auth.guard.ts — remove allowlistGuard (D-13):
    Delete the entire allowlistGuard export block. Remove any import it depends on that is not used elsewhere.

    5. app.routes.ts — replace allowlistGuard usages (D-13):
    Remove allowlistGuard from import. In every canActivate array that had [allowlistGuard, onboardingGuard], remove allowlistGuard leaving just [onboardingGuard]. PATTERNS.md says 4-5 occurrences at lines 81, 90, 99, 108, 113 — read the file to find all occurrences.
  </action>
  <verify>
    <automated>cd packages/dashboard && npm run test -- --watch=false 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - npm run test in packages/dashboard exits 0 (Karma runs, 5 new tests green)
    - auth.service.ts contains "toSignal(this.auth0.isAuthenticated$" — grep returns match
    - auth.service.ts does NOT contain "firstValueFrom(this.auth0.isAuthenticated$)" — grep returns no match
    - auth.service.ts does NOT contain "localhost:3001" — grep returns no match
    - auth.service.ts contains "vd_access_token" localStorage key
    - auth.interceptor.ts contains "catchError" — grep returns match
    - auth.interceptor.ts contains "refreshTokens" — grep returns match
    - auth.service.ts contains "/auth/me" string in initializeUserState() — grep returns match (D-07)
    - auth.guard.ts does NOT contain "allowlistGuard" — grep returns no match
    - app.routes.ts does NOT contain "allowlistGuard" — grep returns no match
    - npm run lint in packages/dashboard exits 0
  </acceptance_criteria>
  <done>All five critical auth bugs fixed. localStorage persistence wired. Interceptor retries on 401. allowlistGuard removed. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | Stores internal JWT + refresh token; XSS risk accepted per D-01 |
| HTTP interceptor → auth-service | Attaches Bearer token to all HttpClient requests |
| onboardingGuard → Supabase | Queries fleet_members after Supabase session is set (RLS enforced) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | localStorage JWT storage | accept | D-01 decision accepts XSS risk; mitigate with Content-Security-Policy on Vercel (deploy config, not this plan) |
| T-02-03-02 | Spoofing | isAuthenticated always-truthy bug | mitigate | Replace computed(firstValueFrom) with toSignal() — returns actual boolean from Auth0 SDK observable |
| T-02-03-03 | Denial of Service | 401 refresh infinite loop | mitigate | Interceptor skips refresh retry when request URL includes '/auth/refresh'; signs out on refresh failure |
| T-02-03-04 | Elevation of Privilege | allowlistGuard bypass | mitigate | D-13: remove allowlistGuard entirely; all protected routes use onboardingGuard which checks real Supabase org/fleet state |
| T-02-03-05 | Spoofing | Supabase RLS with Auth0 JWT | mitigate | supabase.auth.setSession(auth0Token) called after exchange; Supabase verifies Auth0 JWT via third-party provider config (set up manually in Supabase dashboard) |
| T-02-03-06 | Tampering | environment.ts authServiceUrl | accept | URL is build-time config; SSRF not applicable here (client-side code calling a known endpoint) |
</threat_model>

<verification>
After both tasks complete:

1. cd packages/dashboard && npm run test -- --watch=false — exits 0
2. cd packages/dashboard && npm run lint — exits 0
3. grep -rn "localhost:3001" packages/dashboard/src/app/ — must return 0 matches
4. grep -n "toSignal" packages/dashboard/src/app/core/services/auth.service.ts — must return 1+ matches
5. grep -n "allowlistGuard" packages/dashboard/src/app/app.routes.ts — must return 0 matches
6. grep -n "allowlistGuard" packages/dashboard/src/app/core/guards/auth.guard.ts — must return 0 matches
7. grep -n "vd_access_token" packages/dashboard/src/app/core/services/auth.service.ts — must return 1+ matches
</verification>

<success_criteria>
Angular dashboard auth layer fully wired: isAuthenticated is a real boolean signal, localStorage persists tokens, interceptor handles 401 refresh, guards redirect correctly, no hardcoded localhost URLs, allowlistGuard removed. All unit tests pass.
</success_criteria>

<output>
After completion, create .planning/phases/02-auth-fleet-management/02-03-SUMMARY.md
</output>
