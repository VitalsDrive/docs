---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/parser/package.json
  - packages/parser/jest.config.js
  - packages/parser/src/types.ts
  - packages/parser/src/logger.ts
  - packages/parser/src/protocol-parser.ts
  - packages/parser/src/telemetry-writer.ts
  - packages/parser/src/device-auth.ts
  - packages/parser/src/connection-handler.ts
  - packages/parser/src/health.ts
  - packages/parser/src/index.ts
  - packages/parser/tests/protocol-parser.test.ts
  - packages/parser/tests/telemetry-writer.test.ts
  - packages/parser/tests/connection-handler.test.ts
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-05]
tags: [parser, teltonika, codec8e, supabase, pino, jest]

must_haves:
  truths:
    - "Parser TCP server listens on PARSER_PORT (default 5050) and accepts connections"
    - "Parser decodes Codec 8 Extended packets with valid CRC-16-IBM and rejects corrupt packets"
    - "Parser extracts FMC003 IO IDs 7040 (temp), 7044 (rpm), 7045 (speed), 7059 (voltage); IO 7038 (DTC count) parsed but discarded"
    - "Parser writes telemetry rows to Supabase telemetry_logs with vehicle_id resolved via devices.imei lookup, dtc_codes always []"
    - "Parser buffers up to SUPABASE_QUEUE_MAX_SIZE records during Supabase outage, discards oldest when full, drains queue on recovery"
    - "Parser handles duplicate IMEI login by closing previous session (D-11) and discards partial buffer on disconnect (D-10)"
    - "Parser exposes GET /health on HTTP port returning status, activeConnections, queueDepth, lastSupabasePush; 503 when degraded"
    - "All logging uses Pino structured JSON (no console.log) with level controlled by LOG_LEVEL env var"
    - "Jest tests for protocol-parser, telemetry-writer, connection-handler pass; full suite green"
  artifacts:
    - path: "packages/parser/src/types.ts"
      provides: "Shared interfaces: TelemetryPacket, DeviceLookup, ConnectionState, HealthStatus, ParsedAVLRecord"
      exports: ["TelemetryPacket", "DeviceLookup", "ConnectionState", "HealthStatus", "ParsedAVLRecord"]
    - path: "packages/parser/src/logger.ts"
      provides: "Pino singleton logger"
      exports: ["logger"]
    - path: "packages/parser/src/protocol-parser.ts"
      provides: "Pure-function Codec 8 Extended decoder with crc16 and parseCodec8ExtendedPacket"
      exports: ["parseCodec8ExtendedPacket", "crc16"]
    - path: "packages/parser/src/telemetry-writer.ts"
      provides: "TelemetryWriter class with Supabase INSERT, in-memory queue, exponential backoff retry"
      exports: ["TelemetryWriter"]
    - path: "packages/parser/src/device-auth.ts"
      provides: "DeviceAuth class with IMEI validation, devices table lookup, last_seen update"
      exports: ["DeviceAuth"]
    - path: "packages/parser/src/connection-handler.ts"
      provides: "ConnectionHandler class managing TCP sockets, session Map, duplicate IMEI handling"
      exports: ["ConnectionHandler"]
    - path: "packages/parser/src/health.ts"
      provides: "createHealthServer factory returning http.Server for GET /health"
      exports: ["createHealthServer"]
    - path: "packages/parser/src/index.ts"
      provides: "Thin entry point wiring all modules and starting TCP + HTTP servers"
    - path: "packages/parser/jest.config.js"
      provides: "Jest + ts-jest config for CommonJS TypeScript"
    - path: "packages/parser/tests/protocol-parser.test.ts"
      provides: "Unit tests covering packet framing, CRC accept/reject, IO ID 7040/7044/7045/7059 extraction, partial buffer discard"
    - path: "packages/parser/tests/telemetry-writer.test.ts"
      provides: "Unit tests covering Supabase INSERT fields, dtc_codes=[], queue enqueue, discard oldest, 3-failure alert, queue drain on recovery"
    - path: "packages/parser/tests/connection-handler.test.ts"
      provides: "Unit tests covering duplicate IMEI session close (D-11) and session map cleanup on disconnect"
  key_links:
    - from: "packages/parser/src/index.ts"
      to: "packages/parser/src/connection-handler.ts"
      via: "net.createServer(handler.handleConnection)"
      pattern: "new ConnectionHandler"
    - from: "packages/parser/src/connection-handler.ts"
      to: "packages/parser/src/protocol-parser.ts"
      via: "parseCodec8ExtendedPacket(buffer)"
      pattern: "parseCodec8ExtendedPacket"
    - from: "packages/parser/src/connection-handler.ts"
      to: "packages/parser/src/telemetry-writer.ts"
      via: "writer.write(record)"
      pattern: "telemetryWriter\\.write"
    - from: "packages/parser/src/telemetry-writer.ts"
      to: "supabase.telemetry_logs"
      via: "supabase.from('telemetry_logs').insert(...)"
      pattern: "telemetry_logs.*insert"
    - from: "packages/parser/src/device-auth.ts"
      to: "supabase.devices"
      via: "supabase.from('devices').select(...).eq('imei', imei)"
      pattern: "devices.*select.*imei"
    - from: "packages/parser/src/health.ts"
      to: "packages/parser/src/telemetry-writer.ts"
      via: "getStatus() reads writer.queueDepth and writer.lastSupabasePush"
      pattern: "queueDepth"
---

<objective>
Refactor the 337-line monolith `packages/parser/src/index.ts` into testable modules (per D-01), correct the FMC003 IO element IDs (per D-06), add Pino structured logging (per D-04), add a Supabase outage queue with exponential backoff (per D-09), add a GET /health HTTP endpoint (per D-12), and stand up Jest unit tests for the priority modules (per D-02). This plan covers INGEST-01, INGEST-02, INGEST-03, and INGEST-05.

Purpose: Replace the brittle monolith and wrong IO IDs that cause silent data corruption (all rows show temp=0/voltage=0/rpm=0) with a modular, test-covered ingestion server that survives Supabase outages and is observable in production.

Output: Refactored parser package — 8 source modules, 3 test files, 1 Jest config, updated package.json with pino + jest dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-telemetry-pipeline/01-CONTEXT.md
@.planning/phases/01-telemetry-pipeline/01-RESEARCH.md
@.planning/phases/01-telemetry-pipeline/01-PATTERNS.md
@.planning/phases/01-telemetry-pipeline/01-VALIDATION.md
@packages/parser/src/index.ts
@packages/parser/package.json
@packages/parser/tsconfig.json
@CLAUDE.md

<interfaces>
<!-- Existing interfaces in packages/parser/src/index.ts lines 5-28 — extract verbatim into types.ts and extend with HealthStatus and ParsedAVLRecord -->

From packages/parser/src/index.ts (existing, to be moved to src/types.ts):
```typescript
interface TelemetryPacket {
  vehicleId: string;
  imei: string;
  lat: number;
  lng: number;
  speed: number;
  temp: number;
  voltage: number;
  rpm: number;
  dtcCodes: string[];
  timestamp: string;
}

interface DeviceLookup {
  id: string;
  vehicle_id: string | null;
  fleet_id: string;
  status: 'unassigned' | 'active' | 'inactive';
}

interface ConnectionState {
  imei: string | null;
  authenticated: boolean;
}
```

New interfaces to add to types.ts (per D-12 health endpoint):
```typescript
interface HealthStatus {
  status: 'ok' | 'degraded';
  activeConnections: number;
  queueDepth: number;
  lastSupabasePush: string | null;
  degraded: boolean;
}

interface ParsedAVLRecord {
  timestamp: string;          // ISO from device AVL uint64 BE ms-epoch
  lat: number;
  lng: number;
  speed: number;              // km/h
  temp: number;               // °C
  voltage: number;            // V
  rpm: number;
  dtcCount: number;           // IO 7038, parsed but not stored
}
```

FMC003 IO ID hex reference (for buffer reads — uint16 BE):
| Decimal | Hex    | Field             | Scaling            |
|---------|--------|-------------------|--------------------|
| 7038    | 0x1B7E | DTC count         | raw uint, discard  |
| 7040    | 0x1B80 | Coolant temp      | uint16 / 10 ' °C   |
| 7044    | 0x1B84 | Engine RPM        | uint32 raw         |
| 7045    | 0x1B85 | Vehicle speed     | uint16 raw ' km/h  |
| 7059    | 0x1B93 | Control voltage   | uint16 / 1000 ' V  |
</interfaces>

<env_vars>
Required by parser package:
- SUPABASE_URL — Supabase REST endpoint
- SUPABASE_SERVICE_ROLE_KEY — bypasses RLS (existing)
- PARSER_PORT — TCP listen port (default 5050)
- LOG_LEVEL — pino level: trace|debug|info|warn|error|fatal (default info) [D-04]
- SUPABASE_QUEUE_MAX_SIZE — max records buffered during outage (default 1000) [D-09]
- HEALTH_PORT — HTTP port for /health endpoint (default 8080, Railway-compatible) [D-12]
</env_vars>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install deps, extract types/logger, refactor protocol-parser + telemetry-writer with correct FMC003 IDs and Supabase queue, write their tests</name>
  <files>packages/parser/package.json, packages/parser/jest.config.js, packages/parser/src/types.ts, packages/parser/src/logger.ts, packages/parser/src/protocol-parser.ts, packages/parser/src/telemetry-writer.ts, packages/parser/tests/protocol-parser.test.ts, packages/parser/tests/telemetry-writer.test.ts</files>
  <read_first>
    - packages/parser/src/index.ts (current 415-line monolith — extract crc16 lines 31-44, packet framing lines 137-216, getAVLRecordSize/getAVLRecordSizeAt lines 219-255, parseAVLRecord lines 257-333, writeToSupabase lines 390-409)
    - packages/parser/package.json (current deps: @supabase/supabase-js ^2.39.0, dotenv ^16.3.1)
    - packages/parser/tsconfig.json (must remain module: commonjs, strict: true)
    - .planning/phases/01-telemetry-pipeline/01-PATTERNS.md (Pattern Map — IO ID hex table, exact code excerpts to preserve)
    - .planning/phases/01-telemetry-pipeline/01-RESEARCH.md (Patterns 1-3, Pitfalls 1-5, FMC003 IO ID mapping in D-06)
    - .planning/phases/01-telemetry-pipeline/01-CONTEXT.md (D-02, D-03, D-04, D-06, D-07, D-08, D-09)
    - packages/simulator/src/index.ts (buildCodec8ExtendedPacket lines 167-242 — use this exact builder pattern in protocol-parser.test.ts to construct valid test packets)
  </read_first>
  <behavior>
    types.ts:
    - Exports TelemetryPacket, DeviceLookup, ConnectionState verbatim from existing index.ts lines 5-28.
    - Exports HealthStatus { status: 'ok'|'degraded', activeConnections: number, queueDepth: number, lastSupabasePush: string|null, degraded: boolean }.
    - Exports ParsedAVLRecord { timestamp: string, lat: number, lng: number, speed: number, temp: number, voltage: number, rpm: number, dtcCount: number }.

    logger.ts:
    - Default-exports nothing; named-export `logger` = pino({ level: process.env.LOG_LEVEL || 'info' }).
    - Single shared singleton — imported by every module that logs.

    protocol-parser.ts (pure functions, no class, no Supabase client, no socket I/O):
    - Exports `crc16(data: Buffer): number` — copy lines 31-44 of existing index.ts BIT-EXACT (polynomial 0x1021, initial 0x0000, XOR-shift loop).
    - Exports `parseCodec8ExtendedPacket(buffer: Buffer, imei: string): { records: ParsedAVLRecord[], consumed: number, error?: string }`:
      * Scans for 4-byte zero preamble.
      * Reads uint32 BE length at offset 4. totalSize = 8 + dataLength. Returns { records: [], consumed: 0 } when buffer.length < totalSize (partial — caller keeps buffer).
      * Slices payload = packet[8..]. codecId at payload[0] must be 0x8E (else { records: [], consumed: totalSize, error: 'unknown codec' }).
      * numRecords = payload.readUInt16BE(1).
      * Computes crcEndOffset = 1 + 2 + (sum of getAVLRecordSizeAt over numRecords) + 2. CRC = payload.readUInt16BE(crcEndOffset). Compare crc16(payload.slice(0, crcEndOffset)). Mismatch ' { records: [], consumed: totalSize, error: 'CRC mismatch' }.
      * Iterates records starting at offset 3, calling parseAVLRecord (local, not exported).
      * Returns { records, consumed: totalSize }.
    - Local helpers (not exported): `getAVLRecordSizeAt(payload, offset)` (preserve lines 230-255 logic verbatim), `getAVLRecordSize(payload, numRecords)` (preserve lines 219-228 verbatim), `parseAVLRecord(payload, offset, imei)`.
    - parseAVLRecord IO-ID mapping (REPLACE OLD IDs 67/128/179/5 with FMC003 D-06 IDs):
      * Default speed = payload.readUInt16BE(offset + 23) / 10.
      * switch(ioId):
        - case 7040: temp = payload.readUInt16BE(ioOffset) / 10; break;   // °C"10 ' °C
        - case 7044: rpm = payload.readUInt32BE(ioOffset); break;          // raw RPM
        - case 7045: speed = payload.readUInt16BE(ioOffset); break;        // km/h (overrides header)
        - case 7059: voltage = payload.readUInt16BE(ioOffset) / 1000; break; // mV ' V
        - case 7038: dtcCount = payload.readUInt16BE(ioOffset); break;    // parsed but NOT stored (D-07)
      * IO type ' value size switch: preserve lines 308-317 verbatim (1=1, 2=2, 3=4, 4=8, 5=4, 6=8, default=1).
    - Timestamp: use device AVL timestamp — `new Date(Number(payload.readBigUInt64BE(offset))).toISOString()` (NOT new Date().toISOString()) per Pitfall 3 of RESEARCH.md.
    - dtcCodes is NOT in ParsedAVLRecord — caller (telemetry-writer) hardcodes [] per D-07.
    - NO console.* anywhere — use `import { logger } from './logger'` and `logger.warn({ remoteAddress, offset }, 'AVL record too short')` etc.

    telemetry-writer.ts (class):
    - Constructor: `new TelemetryWriter(supabase: SupabaseClient)`. Reads queueMaxSize = parseInt(process.env.SUPABASE_QUEUE_MAX_SIZE || '1000', 10).
    - Private state: queue: TelemetryPacket[] = [], consecutiveFailures = 0, retryCount = 0, lastPushTimestamp: string | null = null, retryTimer: NodeJS.Timeout | null = null.
    - Public getters: `get queueDepth()` returns queue.length; `get lastSupabasePush()` returns lastPushTimestamp; `get degraded()` returns consecutiveFailures >= 3.
    - `async write(record: TelemetryPacket): Promise<void>`:
      * Calls supabase.from('telemetry_logs').insert({ vehicle_id, lat, lng, temp, voltage, rpm, dtc_codes: [], timestamp }). dtc_codes is HARDCODED [] per D-07 (ignore record.dtcCodes if present).
      * On error: consecutiveFailures++, this.enqueue(record), if consecutiveFailures >= 3 logger.error({ consecutiveFailures }, 'Supabase failure alert'), this.scheduleRetry().
      * On success: consecutiveFailures = 0, retryCount = 0, lastPushTimestamp = new Date().toISOString(); if queue.length > 0 schedule immediate drainQueue() via setImmediate.
    - `private enqueue(record)`: if queue.length >= queueMaxSize logger.warn({ queueDepth: queue.length }, 'Queue full — discarding oldest'); queue.shift(); queue.push(record).
    - `private scheduleRetry()`: clears existing retryTimer; delay = Math.min(1000 * 2 ** retryCount, 60000); retryCount++; retryTimer = setTimeout(() => this.drainQueue(), delay).
    - `private async drainQueue()`: while queue.length > 0, dequeue record, attempt insert; on failure re-enqueue at head (unshift) and break, scheduleRetry; on full drain reset retryCount.
    - All logging via pino logger import — no console.*.

    jest.config.js (CommonJS module.exports):
    - preset: 'ts-jest', testEnvironment: 'node', testMatch: ['**/tests/**/*.test.ts'], moduleFileExtensions: ['ts', 'js'].

    protocol-parser.test.ts:
    - Imports parseCodec8ExtendedPacket and crc16 from '../src/protocol-parser'.
    - jest.mock('../src/logger', () => ({ logger: { info: jest.fn(), warn: jest.fn(), error: jest.fn(), debug: jest.fn() } })).
    - Local helper `buildTestPacket(opts)` — replicates simulator's buildCodec8ExtendedPacket logic, parameterized on which IO IDs to include and corruptCrc flag.
    - Test cases (all MUST pass):
      * `crc16` matches simulator output for `Buffer.from([0x8E, 0x00, 0x01])` (compute expected by running same algo).
      * Packet with IO 7040 value 900 ' records[0].temp === 90 (90.0°C).
      * Packet with IO 7044 value 2500 (uint32) ' records[0].rpm === 2500.
      * Packet with IO 7045 value 65 ' records[0].speed === 65.
      * Packet with IO 7059 value 12500 ' records[0].voltage === 12.5.
      * Packet with IO 7038 value 3 ' records[0].dtcCount === 3 (parsed) and dtc_codes is NOT a property of ParsedAVLRecord.
      * Corrupt CRC ' result.error matches /CRC/, records is [].
      * Partial buffer (truncated mid-packet) ' consumed === 0, records: [] (caller must keep buffer).
      * Unknown codec 0x07 ' result.error matches /codec/.
      * Wrong-IO-ID legacy packet (IDs 67/128/179) ' temp/voltage/rpm all 0 (proves new IDs required).

    telemetry-writer.test.ts:
    - jest.mock '@supabase/supabase-js' with createClient returning { from: () => ({ insert: jest.fn() }) } — control insert resolved value per test.
    - jest.mock '../src/logger' as in protocol-parser test.
    - Test cases (all MUST pass):
      * Successful write ' supabase.from('telemetry_logs').insert called once with vehicle_id/lat/lng/temp/voltage/rpm/dtc_codes:[]/timestamp; queue stays empty; lastSupabasePush is ISO string.
      * dtc_codes is ALWAYS [] even when record.dtcCodes = ['P0301'] (proves D-07 hardcoding).
      * Supabase insert rejects ' record enqueued, consecutiveFailures === 1.
      * 3 consecutive failures ' logger.error called with consecutiveFailures: 3 and message matching /failure alert/.
      * Queue size = 1000, attempt 1001th enqueue ' logger.warn /Queue full/, queue stays 1000, oldest discarded.
      * Queue holds N records, Supabase recovers ' drainQueue called, all N flushed, queue empty.
      * Exponential backoff delays: assert setTimeout called with 1000, then 2000, then 4000 ... capped at 60000 (use jest.useFakeTimers and inspect setTimeout mock calls).
  </behavior>
  <action>
    1. Add dependencies — edit packages/parser/package.json:
       - dependencies: add "pino": "^10.3.1"
       - devDependencies: add "jest": "^29.7.0", "ts-jest": "^29.4.9", "@types/jest": "^29.5.12" (pinned to verified-compatible jest@29 + ts-jest@29 pair — ts-jest 29.x officially supports Jest 27-29; Jest 30 requires ts-jest 30 which is not yet GA)
       Run `cd packages/parser && npm install` to write package-lock and node_modules.

    2. Create packages/parser/jest.config.js (CommonJS):
       module.exports = { preset: 'ts-jest', testEnvironment: 'node', testMatch: ['**/tests/**/*.test.ts'], moduleFileExtensions: ['ts', 'js'] };

    3. Create packages/parser/src/types.ts — export TelemetryPacket, DeviceLookup, ConnectionState (verbatim from existing index.ts lines 5-28), HealthStatus, ParsedAVLRecord (shapes per `<interfaces>` block above).

    4. Create packages/parser/src/logger.ts:
       import pino from 'pino';
       export const logger = pino({ level: process.env.LOG_LEVEL || 'info' });

    5. Create packages/parser/src/protocol-parser.ts per behavior block. Preserve crc16 (lines 31-44), getAVLRecordSizeAt (230-255), getAVLRecordSize (219-228) BIT-EXACT from existing index.ts. Replace IO ID switch in parseAVLRecord (lines 293-306) with the FMC003 D-06 mapping (7040/7044/7045/7059/7038). Switch timestamp to device AVL timestamp.

    6. Create packages/parser/src/telemetry-writer.ts per behavior block. Constructor accepts injected SupabaseClient so tests can mock. dtc_codes is HARDCODED `[]` in the insert payload (D-07).

    7. Create packages/parser/tests/protocol-parser.test.ts — copy buildCodec8ExtendedPacket from packages/simulator/src/index.ts lines 167-242 into the test file as a local helper, parameterize on IO IDs and corruptCrc, run all cases listed in behavior block.

    8. Create packages/parser/tests/telemetry-writer.test.ts — mock @supabase/supabase-js per behavior block, use jest.useFakeTimers for backoff assertions.

    Constraints:
    - NEVER use console.* — every log call goes through the pino logger singleton (D-04).
    - NEVER change tsconfig (must stay module: commonjs, strict: true).
    - dtc_codes column written to Supabase is HARDCODED [] in TelemetryWriter — do not read from record.dtcCodes (D-07).
    - Use device AVL timestamp `new Date(Number(payload.readBigUInt64BE(offset))).toISOString()`, NOT server time (Pitfall 3).
    - Do not export OBD2Parser class anywhere — it is deleted in Task 2.
    - All tests MUST mock pino logger to keep output clean.
  </action>
  <verify>
    <automated>cd packages/parser &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --testPathPattern='(protocol-parser|telemetry-writer)' --passWithNoTests=false &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File exists: packages/parser/jest.config.js — grep `preset:.*ts-jest` returns 1 match
    - File exists: packages/parser/src/types.ts — grep `export interface TelemetryPacket` returns 1, `export interface HealthStatus` returns 1, `export interface ParsedAVLRecord` returns 1
    - File exists: packages/parser/src/logger.ts — grep `export const logger = pino` returns 1 match
    - File exists: packages/parser/src/protocol-parser.ts — grep `export function parseCodec8ExtendedPacket` returns 1, `export function crc16` returns 1, `case 7040` returns 1, `case 7044` returns 1, `case 7045` returns 1, `case 7059` returns 1, `case 7038` returns 1; grep `case 67[^0-9]|case 128[^0-9]|case 179[^0-9]` returns 0 (old IDs gone)
    - File exists: packages/parser/src/telemetry-writer.ts — grep `export class TelemetryWriter` returns 1, `dtc_codes:\s*\[\]` returns 1, `SUPABASE_QUEUE_MAX_SIZE` returns 1, `Math\.pow\(2,\s*` returns 1 (exponential backoff), `60000` returns 1 (cap), `consecutiveFailures` count >= 4
    - grep -v '^\s*\*' on all four new src files returns 0 for `console\.(log|error|warn)` (no console.* outside JSDoc)
    - packages/parser/package.json grep `"pino"` returns 1, `"jest"` returns 1, `"ts-jest"` returns 1, `"@types/jest"` returns 1
    - `cd packages/parser && npx tsc --noEmit` exits 0
    - `cd packages/parser && npm test -- --testPathPattern=protocol-parser` exits 0 and stdout shows >= 10 tests passed
    - `cd packages/parser && npm test -- --testPathPattern=telemetry-writer` exits 0 and stdout shows >= 7 tests passed
    - Test file packages/parser/tests/protocol-parser.test.ts — grep `toBe(90)` returns >= 1, `toBe(12.5)` returns >= 1, `dtcCount` returns >= 1
    - Test file packages/parser/tests/telemetry-writer.test.ts — grep `dtc_codes` returns >= 2, `Queue full` returns >= 1, `consecutiveFailures` returns >= 1
  </acceptance_criteria>
  <done>Parser package compiles, pino + jest installed, protocol-parser and telemetry-writer modules extracted with correct FMC003 IO IDs (7040/7044/7045/7059) and Supabase queue with exponential backoff, two test suites green. Old IDs 67/128/179 removed from parser code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor connection-handler + device-auth, add health endpoint, rewire index.ts as thin orchestrator, write connection-handler tests</name>
  <files>packages/parser/src/connection-handler.ts, packages/parser/src/device-auth.ts, packages/parser/src/health.ts, packages/parser/src/index.ts, packages/parser/tests/connection-handler.test.ts, packages/parser/.env.example</files>
  <read_first>
    - packages/parser/src/index.ts (current monolith — extract handleConnection lines 68-92, IMEI login phase lines 102-130, processBuffer dispatch lines 94-216, processTelemetry lines 335-377, updateDeviceLastSeen lines 379-388)
    - packages/parser/src/protocol-parser.ts (created in Task 1 — consumes parseCodec8ExtendedPacket)
    - packages/parser/src/telemetry-writer.ts (created in Task 1 — consumes TelemetryWriter.write, queueDepth, lastSupabasePush, degraded)
    - packages/parser/src/types.ts (created in Task 1 — ConnectionState, DeviceLookup, HealthStatus)
    - packages/parser/src/logger.ts (created in Task 1)
    - packages/parser/.env.example (current env vars; add LOG_LEVEL, SUPABASE_QUEUE_MAX_SIZE, HEALTH_PORT)
    - .planning/phases/01-telemetry-pipeline/01-PATTERNS.md (Pattern 5 session Map, health endpoint pattern)
    - .planning/phases/01-telemetry-pipeline/01-CONTEXT.md (D-10, D-11, D-12, D-13)
  </read_first>
  <behavior>
    device-auth.ts:
    - Class DeviceAuth, constructor `new DeviceAuth(supabase: SupabaseClient)`.
    - `validateImei(buffer: Buffer): { imei: string | null, consumed: number }` — pure synchronous check: needs >= 15 bytes; if first 15 match /^\d{15}$/ returns { imei: imeiStr, consumed: 15 }; if buffer.length > 15 and prefix invalid returns { imei: null, consumed: 15 } (caller logs and discards); else { imei: null, consumed: 0 }.
    - `async lookupDevice(imei: string): Promise<DeviceLookup | null>` — supabase.from('devices').select('id, vehicle_id, fleet_id, status').eq('imei', imei).single(). Returns null on any error, status==='inactive', or vehicle_id===null. Logs warn with reason via pino.
    - `async updateLastSeen(imei: string): Promise<void>` — supabase.from('devices').update({ last_seen: new Date().toISOString() }).eq('imei', imei). Logs error on failure, never throws.

    connection-handler.ts:
    - Class ConnectionHandler. Constructor `new ConnectionHandler(deviceAuth: DeviceAuth, telemetryWriter: TelemetryWriter)`.
    - Private state: sessions: Map<string, net.Socket> = new Map() — imei ' socket.
    - Public getter `get activeConnections()`: returns this.sessions.size (used by health endpoint).
    - `handleConnection(socket: net.Socket): void` — equivalent of existing handleConnection lines 68-92 but delegates and uses logger:
      * Per-socket state: buffer = Buffer.alloc(0), connState: ConnectionState = { imei: null, authenticated: false }, remoteAddress string.
      * socket.on('data', async chunk => { buffer = Buffer.concat([buffer, chunk]); await this.processBuffer(...); }).
      * socket.on('close', () => { if (connState.imei && this.sessions.get(connState.imei) === socket) this.sessions.delete(connState.imei); buffer = Buffer.alloc(0); logger.info({ remoteAddress, imei: connState.imei }, 'connection closed'); }).
      * socket.on('error', err => logger.error({ err: err.message, remoteAddress }, 'socket error')).
    - `private async processBuffer(...)`:
      * Phase 1 (login): if !connState.authenticated, call deviceAuth.validateImei(buffer). If imei returned: connState.imei = imei; connState.authenticated = true; this.onLogin(imei, socket); socket.write(Buffer.from([0x01])); buffer = buffer.slice(consumed); logger.info({ remoteAddress, imei }, 'device authenticated'). Else if consumed === 15 (bad prefix): logger.warn, buffer = buffer.slice(15). Else return (keep buffer).
      * Phase 2 (data): call parseCodec8ExtendedPacket(buffer, connState.imei). If consumed === 0 return (partial). If result.error: logger.warn({ error: result.error, imei }, 'parse error'); buffer = buffer.slice(consumed). Else: for each record, look up device + write — see below.
      * For each ParsedAVLRecord:
        - device = await deviceAuth.lookupDevice(imei). If null, skip record.
        - await deviceAuth.updateLastSeen(imei).
        - Build TelemetryPacket { vehicleId: device.vehicle_id, imei, lat, lng, speed, temp, voltage, rpm, dtcCodes: [], timestamp }.
        - await telemetryWriter.write(packet).
      * After records: ACK with 4-byte BE record count: socket.write(Buffer of records.length).
      * buffer = buffer.slice(consumed).
    - `onLogin(imei: string, socket: net.Socket): void` — D-11: if existing = this.sessions.get(imei) && existing !== socket: logger.warn({ imei }, 'duplicate login — closing previous session'); existing.destroy(); this.sessions.set(imei, socket).

    health.ts:
    - `export function createHealthServer(getStatus: () => HealthStatus): http.Server` — http.createServer responding to GET /health with JSON of HealthStatus; HTTP 503 when status.degraded === true, else 200. 404 for other paths.

    index.ts (REPLACE entirely — thin wiring layer):
    - import 'dotenv/config'; import * as net from 'net'; import { createClient } from '@supabase/supabase-js'; import { DeviceAuth }, { TelemetryWriter }, { ConnectionHandler }, { createHealthServer }, { logger } from './...'.
    - const supabase = createClient(process.env.SUPABASE_URL || '', process.env.SUPABASE_SERVICE_ROLE_KEY || '').
    - const deviceAuth = new DeviceAuth(supabase); const telemetryWriter = new TelemetryWriter(supabase); const handler = new ConnectionHandler(deviceAuth, telemetryWriter).
    - const tcpPort = parseInt(process.env.PARSER_PORT || '5050', 10); const tcpServer = net.createServer(s => handler.handleConnection(s)); tcpServer.listen(tcpPort, () => logger.info({ port: tcpPort }, 'OBD2 parser TCP listening')) (D-13 server start).
    - const healthPort = parseInt(process.env.HEALTH_PORT || '8080', 10); const healthServer = createHealthServer(() => { const failures = telemetryWriter.degraded; return { status: failures ? 'degraded' : 'ok', activeConnections: handler.activeConnections, queueDepth: telemetryWriter.queueDepth, lastSupabasePush: telemetryWriter.lastSupabasePush, degraded: failures }; }); healthServer.listen(healthPort, () => logger.info({ port: healthPort }, 'health HTTP listening')).
    - No exports — entry point only.

    .env.example: append LOG_LEVEL=info, SUPABASE_QUEUE_MAX_SIZE=1000, HEALTH_PORT=8080.

    connection-handler.test.ts:
    - jest.mock '../src/logger' as before.
    - Local mockSocket() helper based on EventEmitter (per PATTERNS.md lines 537-547).
    - Mock DeviceAuth (validateImei jest fn, lookupDevice resolved {vehicle_id: 'v1', ...}, updateLastSeen resolved).
    - Mock TelemetryWriter (write resolved, queueDepth: 0, lastSupabasePush: null, degraded: false).
    - Test cases:
      * Duplicate IMEI login (D-11) — onLogin('123456789012345', s1) then onLogin('123456789012345', s2) ' s1.destroy called exactly once, sessions.get(imei) === s2.
      * Session cleanup on socket close — after authenticating then emitting 'close', sessions.size === 0.
      * activeConnections getter — after 3 successful logins with different IMEIs, handler.activeConnections === 3.
      * Partial buffer discarded on disconnect — socket emits 'close' mid-packet; reconnect with new socket starts fresh buffer (assert by triggering close after partial data write, then sending IMEI on new socket and verifying success — proves no cross-session buffer carry).
  </behavior>
  <action>
    1. Create packages/parser/src/device-auth.ts per behavior block. Preserve devices lookup logic from existing index.ts lines 342-366 (status checks, vehicle_id null check). Preserve updateDeviceLastSeen from lines 379-388. Replace all console.* with logger.

    2. Create packages/parser/src/connection-handler.ts per behavior block. Preserve handleConnection socket event wiring (lines 68-92) and IMEI login logic (102-130). Add sessions Map and onLogin for D-11. On socket close: discard buffer (D-10), delete from sessions Map. Replace all console.* with logger.

    3. Create packages/parser/src/health.ts per behavior block — lightweight http.createServer, no express.

    4. REPLACE packages/parser/src/index.ts entirely with the thin wiring layer per behavior block. The old OBD2Parser class is DELETED. The old `export { OBD2Parser, TelemetryPacket }` line is REMOVED.

    5. Append to packages/parser/.env.example:
       LOG_LEVEL=info
       SUPABASE_QUEUE_MAX_SIZE=1000
       HEALTH_PORT=8080

    6. Create packages/parser/tests/connection-handler.test.ts per behavior block. Use EventEmitter mock sockets, mock DeviceAuth and TelemetryWriter.

    Constraints:
    - NO console.* anywhere in new modules.
    - HEALTH_PORT defaults to 8080 (Railway HTTP); TCP stays on PARSER_PORT=5050 (D-12).
    - On socket 'close', delete the imei key from sessions Map ONLY if sessions.get(imei) === socket (avoid clobbering the new session after a duplicate login).
    - Per D-10, buffer is local to each socket — when socket closes, the buffer variable goes out of scope automatically. Explicitly set buffer = Buffer.alloc(0) in the close handler to prevent any closure-held reference holding bytes (defensive).
    - The TCP server listens on the SAME PORT as before (PARSER_PORT, default 5050) — Railway deployment expects this. Health HTTP is on a SEPARATE port (HEALTH_PORT, default 8080).
    - The TCP ACK sent after successful packet processing MUST be 4-byte BE = records.length (preserving existing protocol behavior from line 213).
    - Per D-13, log structured events: server start (both TCP and HTTP), device connect, device disconnect, login received, login rejected, parse error, CRC failure, Supabase push result (handled in TelemetryWriter), queue depth warning (handled in TelemetryWriter).
  </action>
  <verify>
    <automated>cd packages/parser &amp;&amp; npx tsc --noEmit &amp;&amp; npm test &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File exists: packages/parser/src/connection-handler.ts — grep `export class ConnectionHandler` returns 1, `sessions:\s*Map<string,\s*net\.Socket>` returns 1, `existing\.destroy\(\)` returns 1, `get activeConnections` returns 1
    - File exists: packages/parser/src/device-auth.ts — grep `export class DeviceAuth` returns 1, `from\('devices'\)` returns >= 1, `last_seen` returns 1
    - File exists: packages/parser/src/health.ts — grep `export function createHealthServer` returns 1, `req\.url === '/health'` returns 1, `503` returns 1
    - packages/parser/src/index.ts — grep `class OBD2Parser` returns 0 (class deleted), grep `new ConnectionHandler` returns 1, grep `createHealthServer` returns 1, grep `HEALTH_PORT` returns 1, grep `PARSER_PORT` returns 1
    - packages/parser/.env.example — grep `LOG_LEVEL=` returns 1, grep `SUPABASE_QUEUE_MAX_SIZE=` returns 1, grep `HEALTH_PORT=` returns 1
    - grep -v '^\s*\*' across packages/parser/src/*.ts returns 0 for `console\.(log|error|warn)` (no console.* in production code anywhere in parser)
    - `cd packages/parser && npx tsc --noEmit` exits 0
    - `cd packages/parser && npm test` exits 0 with full suite green (all three test files pass)
    - Test file packages/parser/tests/connection-handler.test.ts — grep `duplicate` returns >= 1, grep `destroy` returns >= 2, grep `activeConnections` returns >= 1
    - Process smoke: `cd packages/parser && timeout 5 npx ts-node src/index.ts &` followed by `curl -sf http://localhost:8080/health` returns JSON containing `"status":"ok"` and HTTP 200 (manual verification via run-and-curl; if timeout binary unavailable on Windows use PowerShell `Start-Process` with `-PassThru` and kill after curl)
  </acceptance_criteria>
  <done>Parser fully refactored to 8 modules with zero console.* usage. TCP server on PARSER_PORT and health HTTP on HEALTH_PORT both start cleanly. Duplicate IMEI closes previous session. Session Map cleans up on disconnect. Full Jest suite green. GET /health returns 200 with status object when healthy, 503 when degraded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet ' TCP :5050 | Untrusted device traffic enters parser; framing, CRC, IMEI all unauthenticated until validated |
| Parser ' Supabase | Service-role key bypasses RLS; full table access from a long-running internet-facing process |
| Parser process ' Health HTTP :8080 | Operator/Railway probes; no auth on /health endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | IMEI handshake (device-auth.ts validateImei) | mitigate | Validate IMEI matches /^\d{15}$/ (Task 2 device-auth.ts). Reject and slice 15 bytes on invalid prefix. Unknown IMEIs (no row in devices table) cause record to be skipped — Task 2 connection-handler.ts via deviceAuth.lookupDevice returning null. |
| T-01-02 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY exposure | mitigate | Key read only from process.env (Task 2 index.ts createClient). Never logged. .env.example contains placeholder only. No DEBUG_RAW_PACKETS flag added in this phase. |
| T-01-03 | DoS | Session Map memory exhaustion via repeated connect/disconnect or duplicate IMEI floods | mitigate | Session Map keyed by IMEI (bounded by registered devices count). Duplicate IMEI closes previous (D-11, Task 2 onLogin). On socket close, key deleted from Map (Task 2 connection-handler.ts close handler). |
| T-01-04 | Tampering | Malformed packet flood / CRC manipulation | mitigate | crc16 validation in protocol-parser.ts (Task 1) — CRC mismatch returns error and consumes packet length so caller advances past the bad packet without infinite reparse. Preamble scan with buffer discard >100 bytes prevents unbounded growth (preserved from existing line 148). |
| T-01-05 | DoS | Unbounded queue growth during Supabase outage | mitigate | SUPABASE_QUEUE_MAX_SIZE cap (default 1000) in telemetry-writer.ts (Task 1). On overflow, shift oldest. Exponential backoff prevents tight-loop retries (Task 1 scheduleRetry, cap 60s). |
| T-01-06 | Information Disclosure | Health endpoint leaks internal state to unauthenticated callers | accept | Phase 1 scope: /health is required for Railway probe and exposes only counts and an ISO timestamp — no PII, no credentials. ASVS L1 does not require authentication on operational health probes. Re-evaluate when adding metrics endpoint (post-MVP). |
| T-01-07 | Repudiation | Lack of audit trail for parse failures / device rejections | mitigate | Pino structured JSON logging (D-04, Task 1 logger.ts) with logger.warn/error including imei + remoteAddress for parse errors, CRC failures, login rejection, queue overflow (D-13). Logs captured by Railway; LOG_LEVEL configurable. |

No HIGH severity threats remain unmitigated. All threats either mitigated within the plan tasks or accepted with justification.
</threat_model>

<verification>
1. `cd packages/parser && npx tsc --noEmit` exits 0.
2. `cd packages/parser && npm test` exits 0; all three test files green; total tests >= 17.
3. grep across packages/parser/src/*.ts — `console.(log|warn|error)` returns 0 matches.
4. grep across packages/parser/src/*.ts — `case 67[^0-9]|case 128[^0-9]|case 179[^0-9]` returns 0 (old wrong IO IDs gone).
5. grep across packages/parser/src/*.ts — `case 7040|case 7044|case 7045|case 7059|case 7038` returns 5 matches (D-06 IDs present).
6. Manual smoke: start parser, `curl http://localhost:8080/health` returns HTTP 200 + JSON with status, activeConnections, queueDepth, lastSupabasePush, degraded.
7. Manual smoke: `nc localhost 5050` accepts connection (INGEST-01).
8. Reachability check: every must_have artifact has a creation step in Task 1 or Task 2 — verified by `<files>` enumeration.
</verification>

<success_criteria>
- All 4 phase requirements covered: INGEST-01 (TCP listen — index.ts wiring), INGEST-02 (protocol decode with correct FMC003 IDs — protocol-parser.ts + tests), INGEST-03 (Supabase write with dtc_codes=[] — telemetry-writer.ts + tests), INGEST-05 (queue + duplicate IMEI + partial buffer — telemetry-writer.ts + connection-handler.ts + tests).
- Full Jest suite green: `cd packages/parser && npm test` exits 0 with >= 17 passing tests across 3 files.
- Zero console.* calls in packages/parser/src/*.ts (D-04 enforced).
- Zero references to old IO IDs (67/128/179) in protocol-parser.ts; 5 references to new FMC003 IDs (7038/7040/7044/7045/7059).
- /health endpoint reachable and returns valid HealthStatus JSON.
- All threats in the STRIDE register are mitigated or have a documented disposition.
</success_criteria>

<output>
After completion, create `.planning/phases/01-telemetry-pipeline/01-01-SUMMARY.md` summarizing:
- 8 source modules created (types, logger, protocol-parser, telemetry-writer, device-auth, connection-handler, health, index)
- 3 test files created with N passing tests
- FMC003 IO IDs corrected (7040/7044/7045/7059, with 7038 parsed-not-stored)
- Pino logging, in-memory queue with exponential backoff, GET /health endpoint
- Dependencies added: pino, jest, ts-jest, @types/jest
- Any deviations from D-01 through D-13 and rationale
</output>
